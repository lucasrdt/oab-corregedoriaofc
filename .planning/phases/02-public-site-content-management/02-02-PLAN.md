---
phase: 02-public-site-content-management
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/pages/admin/components/SubsectionsEditor.tsx
  - src/pages/admin/AdminLayout.tsx
  - src/pages/admin/SiteEditor.tsx
autonomous: true
requirements:
  - SUB-02
  - ADM-01
  - ADM-03

must_haves:
  truths:
    - "Admin vê item 'Subseções' na sidebar do painel"
    - "Admin pode criar uma subseção preenchendo cidade, corregedor, endereço, telefone e email"
    - "Admin pode editar uma subseção existente via dialog"
    - "Admin pode excluir uma subseção com confirmação"
    - "Após qualquer mutação (create/edit/delete), a lista atualiza automaticamente e o site público reflete a mudança"
    - "Outros itens da sidebar (Geral, Equipe, FAQ, Agenda, Notícias, Avançado) continuam funcionando sem regressão"
  artifacts:
    - path: "src/pages/admin/components/SubsectionsEditor.tsx"
      provides: "CRUD editor de subseções standalone (sem config/updateConfig)"
      exports: ["default SubsectionsEditor"]
    - path: "src/pages/admin/AdminLayout.tsx"
      provides: "navItems com 'Subseções' adicionado"
    - path: "src/pages/admin/SiteEditor.tsx"
      provides: "Switch renderiza SubsectionsEditor quando activeSection === 'subsections'"
  key_links:
    - from: "src/pages/admin/SiteEditor.tsx"
      to: "src/pages/admin/components/SubsectionsEditor.tsx"
      via: "activeSection === 'subsections'"
      pattern: "activeSection.*subsections"
    - from: "src/pages/admin/components/SubsectionsEditor.tsx"
      to: "supabase.from('subsections')"
      via: "useMutation + useQuery"
      pattern: "from.*subsections"
---

<objective>
Construir o editor de subseções no painel admin (CRUD completo) e conectá-lo à sidebar.

Purpose: ADM-01 e SUB-02 — o admin precisa gerenciar subseções municipais sem precisar de desenvolvedor.
Output: SubsectionsEditor.tsx funcional, sidebar com novo item, SiteEditor roteando para o novo editor. ADM-03 garantido — seções existentes não regridem.
</objective>

<execution_context>
@C:/Users/gusta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-public-site-content-management/02-RESEARCH.md
@.planning/phases/02-public-site-content-management/02-01-SUMMARY.md

<interfaces>
<!-- Interfaces críticas. Extraídas do código existente. -->

From src/pages/admin/AdminLayout.tsx (navItems array — adicionar sem remover existentes):
```typescript
const navItems = [
  { id: 'general',  label: 'Geral & SEO' },
  { id: 'content',  label: 'Quem Somos' },
  { id: 'team',     label: 'Equipe' },
  { id: 'cases',    label: 'Casos' },
  { id: 'faq',      label: 'FAQ' },
  { id: 'leads',    label: 'Leads' },
  { id: 'calendar', label: 'Agenda' },
  { id: 'articles', label: 'Notícias' },
  { id: 'advanced', label: 'Avançado' },
  // ADD HERE:
  { id: 'subsections', label: 'Subseções' },
];
```

From src/pages/admin/SiteEditor.tsx (section render pattern — adicionar sem quebrar existentes):
```typescript
// O SiteEditor renderiza com if/else baseado em activeSection
// Adicionar ANTES do bloco else ou ao final do chain de condicionais:
{activeSection === 'subsections' && (
  <SubsectionsEditor />
)}
// NÃO modificar as condicionais existentes (general, content, team, cases, etc.)
```

From src/pages/admin/components/TeamEditor.tsx (padrão a seguir — mas SubsectionsEditor é standalone sem config prop):
```typescript
// TeamEditor usa config/updateConfig da prop (JSONB)
// SubsectionsEditor NÃO usa config/updateConfig — usa diretamente Supabase + react-query
// Props: SubsectionsEditor não precisa de props (siteId não é necessário para subsections)
```

From src/hooks/useSubsections.ts (criado no Plano 01):
```typescript
export interface Subsection {
  id: string;
  city: string;
  corregedor: string;
  address?: string;
  phone?: string;
  email?: string;
  created_at: string;
}
export function useSubsections(): UseQueryResult<Subsection[]>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar SubsectionsEditor.tsx (CRUD standalone)</name>
  <files>src/pages/admin/components/SubsectionsEditor.tsx</files>
  <action>
    Criar SubsectionsEditor.tsx seguindo o padrão visual de TeamEditor.tsx mas usando Supabase + react-query diretamente (sem config/updateConfig props).

    Estrutura do componente:
    - Importar `useSubsections` e tipo `Subsection` de `@/hooks/useSubsections`
    - Importar `useMutation`, `useQueryClient` de `@tanstack/react-query`
    - Importar `supabase` de `@/lib/supabase`
    - Importar `toast` de `sonner`
    - Importar componentes shadcn: Button, Input, Label, Textarea, Card/CardContent/CardHeader/CardTitle, Dialog/DialogContent/DialogHeader/DialogTitle/DialogTrigger
    - Importar ícones lucide-react: Plus, Trash, Edit

    Estado local: `isDialogOpen: boolean`, `editingItem: Subsection | null`

    Formulário (Dialog): campos com Label + Input para:
      - city (required, label: "Cidade")
      - corregedor (required, label: "Corregedor(a)")
      - address (opcional, label: "Endereço")
      - phone (opcional, label: "Telefone")
      - email (opcional, label: "E-mail", type="email")

    Mutations (cada uma chama `queryClient.invalidateQueries({ queryKey: ['subsections'] })` no onSuccess):
    - `createSubsection`: `supabase.from('subsections').insert([formData])` — toast.success('Subseção criada.')
    - `updateSubsection`: `supabase.from('subsections').update(formData).eq('id', id)` — toast.success('Subseção atualizada.')
    - `deleteSubsection`: `supabase.from('subsections').delete().eq('id', id)` — toast.success('Subseção removida.') com window.confirm antes de chamar a mutation

    onError em todas as mutations: `toast.error('Erro ao salvar. Tente novamente.')`

    handleSubmit: lê FormData do form, chama create ou update conforme `editingItem` ser null ou não, fecha dialog no onSuccess

    Lista: grid `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4` — cada Card mostra city como título, corregedor como subtítulo, address/phone/email como detalhes. Botões Edit e Delete com opacity-0 group-hover:opacity-100 (igual TeamEditor).

    Estado loading (isLoading do useSubsections): exibir "Carregando..." centrado.
    Estado vazio: "Nenhuma subseção cadastrada. Clique em Adicionar para começar."
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "SubsectionsEditor" || echo "sem erros TypeScript em SubsectionsEditor"</automated>
  </verify>
  <done>SubsectionsEditor.tsx compila sem erros TypeScript e implementa create/edit/delete com invalidação de cache</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar Subseções à sidebar e ao SiteEditor</name>
  <files>src/pages/admin/AdminLayout.tsx, src/pages/admin/SiteEditor.tsx</files>
  <action>
    Em src/pages/admin/AdminLayout.tsx:
    - Localizar o array `navItems` (linhas ~17-27)
    - Adicionar `{ id: 'subsections', label: 'Subseções' }` APÓS o item `{ id: 'team', label: 'Equipe' }` e ANTES de `{ id: 'cases', label: 'Casos' }`
    - Não remover nem reordenar nenhum item existente

    Em src/pages/admin/SiteEditor.tsx:
    - Adicionar import: `import SubsectionsEditor from './components/SubsectionsEditor';`
    - Localizar onde as seções são renderizadas condicionalmente (bloco grande de `{activeSection === 'general' && ...}`, `{activeSection === 'team' && ...}` etc.)
    - Adicionar bloco para subsections APÓS o bloco do 'team' e ANTES do bloco do 'cases':
      ```tsx
      {activeSection === 'subsections' && (
        <SubsectionsEditor />
      )}
      ```
    - NÃO modificar nenhum bloco condicional existente — adicição pura

    Verificar ADM-03: confirmar que activeSection === 'calendar', 'articles', 'faq' ainda renderizam CalendarEditor, ArticlesEditor, FaqEditor respectivamente (apenas inspeção visual — não alterar esses blocos).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "AdminLayout|SiteEditor" || echo "sem erros TypeScript"</automated>
  </verify>
  <done>AdminLayout.tsx tem 'subsections' no navItems; SiteEditor.tsx renderiza SubsectionsEditor quando activeSection === 'subsections'; todos os outros itens do nav continuam funcionando</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verificar editor de subseções no painel admin</name>
  <what-built>CRUD de subseções no painel admin, sidebar atualizada, sem regressão nas seções existentes</what-built>
  <how-to-verify>
    1. Execute `npm run dev` e acesse http://localhost:5173/admin (faça login)
    2. No menu lateral, confirme que "Subseções" aparece entre "Equipe" e "Casos"
    3. Clique em "Subseções" — a área de conteúdo deve renderizar o SubsectionsEditor
    4. Teste criar uma subseção: clique "Adicionar", preencha cidade e corregedor, salve — confirme que aparece na lista
    5. Teste editar: clique no ícone de edição de uma subseção, modifique um campo, salve — confirme que a lista atualiza
    6. Teste excluir: clique no ícone de lixeira, confirme o dialog, confirme que a subseção desaparece
    7. Acesse http://localhost:5173/subsecoes — confirme que as subseções criadas aparecem nos cards
    8. Teste regressão ADM-03: clique em "Agenda", "Notícias", "FAQ" na sidebar — cada um deve renderizar seu editor respectivo sem área em branco
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passa sem erros em SubsectionsEditor.tsx, AdminLayout.tsx, SiteEditor.tsx
- navItems em AdminLayout.tsx inclui `{ id: 'subsections', label: 'Subseções' }`
- SiteEditor.tsx tem bloco `activeSection === 'subsections'`
- Seções existentes (calendar, articles, faq, team) não foram modificadas
</verification>

<success_criteria>
- Admin cria/edita/exclui subseções no painel e as mudanças aparecem imediatamente em /subsecoes
- Nenhuma seção existente da sidebar regride
- TypeScript compila sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-public-site-content-management/02-02-SUMMARY.md`
</output>
