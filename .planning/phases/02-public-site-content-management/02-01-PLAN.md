---
phase: 02-public-site-content-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Subsecoes.tsx
  - src/hooks/useSubsections.ts
  - src/components/Header.tsx
  - src/App.tsx
autonomous: true
requirements:
  - SUB-01
  - SUB-03

must_haves:
  truths:
    - "Visitante acessa /subsecoes e vê cards com cidade, corregedor(a), endereço, telefone e e-mail de cada subseção"
    - "O item 'Subseções' aparece na navegação desktop e mobile substituindo 'Casos / Empresas'"
    - "Enquanto os dados carregam, o usuário vê um estado de loading (skeleton ou spinner)"
    - "Se a tabela ainda não tiver dados, a página exibe mensagem vazia em vez de quebrar"
  artifacts:
    - path: "src/pages/Subsecoes.tsx"
      provides: "Página pública /subsecoes com grid de cards"
      exports: ["default Subsecoes"]
    - path: "src/hooks/useSubsections.ts"
      provides: "react-query hook que lê tabela subsections"
      exports: ["useSubsections"]
    - path: "src/components/Header.tsx"
      provides: "Navegação atualizada com link direto para /subsecoes"
  key_links:
    - from: "src/pages/Subsecoes.tsx"
      to: "supabase.from('subsections')"
      via: "useSubsections hook"
      pattern: "useSubsections"
    - from: "src/App.tsx"
      to: "src/pages/Subsecoes.tsx"
      via: "Route path=/subsecoes"
      pattern: "path.*subsecoes"
    - from: "src/components/Header.tsx"
      to: "/subsecoes"
      via: "Link to=/subsecoes"
      pattern: "Link.*subsecoes"
---

<objective>
Criar a página pública `/subsecoes` com cards dinâmicos lidos do Supabase e atualizar a navegação do site para incluir o link "Subseções" substituindo o dropdown "Casos / Empresas".

Purpose: SUB-01 e SUB-03 — visitantes precisam acessar informações de contato de cada subseção municipal, e a navegação precisa refletir isso.
Output: Rota /subsecoes funcional, hook useSubsections, Header atualizado.
</objective>

<execution_context>
@C:/Users/gusta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-public-site-content-management/02-RESEARCH.md

<interfaces>
<!-- Interfaces críticas para o executor. Extraídas diretamente do código. -->

From src/hooks/ (pattern — new hooks follow same shape):
```typescript
// Existing pattern from react-query v5 in codebase
import { useQuery } from '@tanstack/react-query';
import { supabase } from '@/lib/supabase';
// queryFn throws on error (supabase pattern: if (error) throw error)
```

From src/components/Header.tsx (existing nav structure):
```typescript
// Desktop nav: <nav className="hidden lg:flex items-center gap-8">
//   Uses <Link to="..."> for simple links
//   Uses <button> + dropdown for "Casos / Empresas" — REMOVE this entire block
// Mobile nav: separate <div className="border-t border-border pt-4 mt-2">
//   Has "Casos / Empresas" section — REMOVE this entire block too
// Both locations must be updated or mobile nav will still show the old item
```

From src/App.tsx (route registration pattern):
```typescript
// Public routes wrapped in PageTransition:
// <Route path="/equipe" element={<PageTransition><Equipe /></PageTransition>} />
// Follow same pattern for /subsecoes
```

Supabase table schema (must be created before this plan runs):
```sql
-- Create in Supabase Dashboard SQL Editor FIRST (Wave 0 manual step)
CREATE TABLE IF NOT EXISTS public.subsections (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  city       text NOT NULL,
  corregedor text NOT NULL,
  address    text,
  phone      text,
  email      text,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
ALTER TABLE public.subsections ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Public read" ON public.subsections FOR SELECT USING (true);
CREATE POLICY "Auth write" ON public.subsections FOR ALL USING (auth.role() = 'authenticated');
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 0: Criar tabela subsections no Supabase Dashboard</name>
  <what-built>Nenhuma automação possível — tabela precisa existir antes que qualquer código funcione</what-built>
  <how-to-verify>
    1. Acesse o Supabase Dashboard do projeto OAB-MA
    2. Vá em "SQL Editor" e execute o seguinte SQL:

    ```sql
    CREATE TABLE IF NOT EXISTS public.subsections (
      id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
      city       text NOT NULL,
      corregedor text NOT NULL,
      address    text,
      phone      text,
      email      text,
      created_at timestamptz DEFAULT now(),
      updated_at timestamptz DEFAULT now()
    );
    ALTER TABLE public.subsections ENABLE ROW LEVEL SECURITY;
    CREATE POLICY "Public read" ON public.subsections FOR SELECT USING (true);
    CREATE POLICY "Auth write" ON public.subsections FOR ALL USING (auth.role() = 'authenticated');
    ```

    3. Confirme que a tabela aparece em "Table Editor" com as colunas corretas
    4. Opcionalmente insira 1-2 subseções de teste para verificar a página depois
  </how-to-verify>
  <resume-signal>Digite "tabela criada" após criar a tabela no Supabase Dashboard</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Hook useSubsections e página Subsecoes.tsx</name>
  <files>src/hooks/useSubsections.ts, src/pages/Subsecoes.tsx</files>
  <action>
    Criar src/hooks/useSubsections.ts com:
    - `useSubsections()` usando `useQuery` do @tanstack/react-query v5
    - queryKey: ['subsections']
    - queryFn: `supabase.from('subsections').select('*').order('city', { ascending: true })`
    - Lança erro se Supabase retornar erro (padrão `if (error) throw error`)
    - Exportar tipo `Subsection` inline: `{ id: string; city: string; corregedor: string; address?: string; phone?: string; email?: string; created_at: string }`

    Criar src/pages/Subsecoes.tsx com:
    - Importa `useSubsections` e usa `data`, `isLoading`, `error` retornados
    - Estrutura: Header do site (`<Header />`), `<Footer />`, conteúdo principal
    - Estado loading: exibir grid com 6 skeleton cards (div com `animate-pulse bg-muted rounded-xl h-48`)
    - Estado erro: exibir mensagem "Erro ao carregar subseções. Tente novamente mais tarde."
    - Estado vazio (`data.length === 0`): exibir "Nenhuma subseção cadastrada ainda."
    - Estado preenchido: grid responsivo `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
    - Cada card usa `<Card>` do shadcn/ui com: título = `city` (text-lg font-semibold), subtítulo = `corregedor` (text-muted-foreground), linhas de detalhe com ícones do lucide-react: MapPin para address, Phone para phone, Mail para email — omitir linha se campo for nulo/vazio
    - Título da página: `<h1>` "Subseções Municipais" com descrição breve
    - SiteContext (`useSite()`) pode ser usado para logo/footer se necessário, seguindo padrão do restante do site
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "Subsecoes|useSubsections" || echo "sem erros TypeScript nesses arquivos"</automated>
  </verify>
  <done>src/hooks/useSubsections.ts exporta useSubsections e tipo Subsection; src/pages/Subsecoes.tsx renderiza corretamente os três estados (loading, vazio, preenchido)</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar Header e registrar rota /subsecoes</name>
  <files>src/components/Header.tsx, src/App.tsx</files>
  <action>
    Em src/components/Header.tsx:
    - Remover os estados `dropdownOpen` e `setDropdownOpen`
    - Remover a constante `FALLBACK_CASOS` e `casosEmpresas`
    - Remover a importação de `ChevronDown` (não é mais necessária)
    - No desktop nav (className="hidden lg:flex"): remover o bloco `<div className="relative">...</div>` inteiro que contém o button "Casos / Empresas" e o dropdown. Substituir por um único `<Link to="/subsecoes">Subseções</Link>` com as mesmas classes dos outros links de navegação
    - No mobile nav: remover o bloco `<div className="border-t border-border pt-4 mt-2">` que exibe "Casos / Empresas" com os links dos casos. Substituir por `<Link to="/subsecoes" ... onClick={() => setMobileMenuOpen(false)}>Subseções</Link>` com as mesmas classes dos outros links mobile
    - Manter todos os outros itens de nav inalterados (Home, Equipe, Na Mídia, Dúvidas, Contato)

    Em src/App.tsx:
    - Adicionar import: `import Subsecoes from "./pages/Subsecoes";`
    - Adicionar rota dentro de `<Routes>` nas rotas públicas: `<Route path="/subsecoes" element={<PageTransition><Subsecoes /></PageTransition>} />`
    - Posicionar a rota ANTES da rota catch-all `/:slug` para evitar interceptação
    - Não remover as rotas existentes de `/recuperacao-judicial`, `/falencia` etc. (manter por compatibilidade)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "Header|App.tsx" || echo "sem erros TypeScript"</automated>
  </verify>
  <done>Header não contém mais dropdown "Casos / Empresas"; link "Subseções" aparece em desktop e mobile nav; rota /subsecoes registrada no App.tsx</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verificar página /subsecoes e navegação</name>
  <what-built>Página pública /subsecoes com cards de subseções municipais e Header atualizado</what-built>
  <how-to-verify>
    1. Execute `npm run dev` e acesse http://localhost:5173
    2. No header desktop, confirme que "Subseções" aparece como link direto (sem dropdown)
    3. Clique em "Subseções" — deve navegar para /subsecoes sem erro 404
    4. Se inseriu dados de teste no Task 0: confirme que cards aparecem com cidade, corregedor, endereço, telefone e email
    5. Se tabela vazia: confirme mensagem "Nenhuma subseção cadastrada ainda."
    6. Redimensione para mobile: confirme que "Subseções" aparece no menu mobile e "Casos / Empresas" não aparece mais
    7. Verifique que outros links do header (Home, Equipe, etc.) continuam funcionando
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passa sem erros nos arquivos modificados
- Navegação do site não tem mais dropdown "Casos / Empresas"
- Rota /subsecoes existe no App.tsx e renderiza Subsecoes.tsx
- Hook useSubsections usa react-query com queryKey ['subsections']
</verification>

<success_criteria>
- Visitante acessa /subsecoes e vê cards ou mensagem vazia (não quebra)
- Item "Subseções" aparece no nav desktop e mobile
- Dropdown "Casos / Empresas" removido completamente (desktop e mobile)
- TypeScript compila sem erros nos arquivos modificados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-public-site-content-management/02-01-SUMMARY.md`
</output>
