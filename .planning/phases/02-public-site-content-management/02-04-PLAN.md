---
phase: 02-public-site-content-management
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/pages/admin/components/CoursesEditor.tsx
  - src/pages/admin/AdminLayout.tsx
  - src/pages/admin/SiteEditor.tsx
autonomous: true
requirements:
  - ADM-02

must_haves:
  truths:
    - "Admin vê item 'Cursos' na sidebar do painel"
    - "Admin pode criar um curso preenchendo título, descrição, data/hora, modalidade, local, link de inscrição e imagem"
    - "Admin pode editar um curso existente via dialog"
    - "Admin pode excluir um curso com confirmação"
    - "Após qualquer mutação, a lista atualiza automaticamente e /cursos reflete a mudança"
    - "Upload de imagem do curso funciona via componente ImageUpload existente"
  artifacts:
    - path: "src/pages/admin/components/CoursesEditor.tsx"
      provides: "CRUD editor de cursos com upload de imagem"
      exports: ["default CoursesEditor"]
    - path: "src/pages/admin/AdminLayout.tsx"
      provides: "navItems com 'Cursos' adicionado"
    - path: "src/pages/admin/SiteEditor.tsx"
      provides: "Switch renderiza CoursesEditor quando activeSection === 'courses'"
  key_links:
    - from: "src/pages/admin/components/CoursesEditor.tsx"
      to: "supabase.from('courses')"
      via: "useMutation + useQuery"
      pattern: "from.*courses"
    - from: "src/pages/admin/components/CoursesEditor.tsx"
      to: "src/components/ui/image-upload.tsx"
      via: "ImageUpload component"
      pattern: "ImageUpload"
---

<objective>
Construir o editor de cursos no painel admin (CRUD com upload de imagem) e conectá-lo à sidebar.

Purpose: ADM-02 — o admin gerencia cursos e eventos com imagem sem precisar de desenvolvedor.
Output: CoursesEditor.tsx com ImageUpload, sidebar com item "Cursos", SiteEditor roteando para o novo editor.
</objective>

<execution_context>
@C:/Users/gusta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-public-site-content-management/02-RESEARCH.md
@.planning/phases/02-public-site-content-management/02-02-SUMMARY.md
@.planning/phases/02-public-site-content-management/02-03-SUMMARY.md

<interfaces>
<!-- Interfaces críticas. Extraídas do código. -->

From src/components/ui/image-upload.tsx:
```typescript
interface ImageUploadProps {
    value?: string;
    onChange: (url: string) => void;
    disabled?: boolean;
    label?: string;
    bucket?: string;       // default "site-assets"
    siteId?: string;       // prioridade sobre contexto
}
// Uso no editor:
// <ImageUpload siteId={OAB_MA_SITE_ID} label="Imagem do Curso" value={imageUrl} onChange={setImageUrl} />
```

From src/hooks/useCourses.ts (criado no Plano 03):
```typescript
export interface Course {
  id: string;
  title: string;
  description?: string;
  date: string;         // timestamptz — enviar ISO string no insert/update
  modality?: string;
  location?: string;
  registration_link?: string;
  image_url?: string;
  created_at: string;
}
export function useCourses(): UseQueryResult<Course[]>
```

From src/pages/admin/AdminLayout.tsx (estado após Plano 02):
```typescript
// navItems já tem 'subsections' adicionado — adicionar 'courses' APÓS 'subsections':
{ id: 'subsections', label: 'Subseções' },  // já existe
{ id: 'courses',     label: 'Cursos' },     // ADICIONAR
```

Form fields para CoursesEditor (usar react-hook-form + zod — mais campos que SubsectionsEditor):
```typescript
// Zod schema:
const courseSchema = z.object({
  title:             z.string().min(3, 'Título obrigatório'),
  description:       z.string().optional(),
  date:              z.string().min(1, 'Data obrigatória'),  // input type="datetime-local"
  modality:          z.string().optional(),
  location:          z.string().optional(),
  registration_link: z.string().url('URL inválida').optional().or(z.literal('')),
});
// image_url gerenciado por useState separado (igual TeamEditor para photo)
```

OAB_MA_SITE_ID constant (de SiteEditor.tsx — redeclarar ou importar):
```typescript
const OAB_MA_SITE_ID = import.meta.env.VITE_SITE_ID || 'CONFIGURE_SITE_ID';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar CoursesEditor.tsx (CRUD com ImageUpload)</name>
  <files>src/pages/admin/components/CoursesEditor.tsx</files>
  <action>
    Criar CoursesEditor.tsx como componente standalone (sem props config/updateConfig).

    Imports necessários:
    - `useCourses`, `Course` de `@/hooks/useCourses`
    - `useMutation`, `useQueryClient` de `@tanstack/react-query`
    - `supabase` de `@/lib/supabase`
    - `toast` de `sonner`
    - `useForm` de `react-hook-form`, `zodResolver` de `@hookform/resolvers/zod`, `z` de `zod`
    - `ImageUpload` de `@/components/ui/image-upload`
    - Componentes shadcn: Button, Input, Label, Textarea, Card/CardContent/CardHeader/CardTitle, Dialog/DialogContent/DialogHeader/DialogTitle/DialogTrigger, Select/SelectContent/SelectItem/SelectTrigger/SelectValue
    - Ícones lucide-react: Plus, Trash, Edit, Calendar, MapPin, ExternalLink

    Constante local: `const OAB_MA_SITE_ID = import.meta.env.VITE_SITE_ID || 'CONFIGURE_SITE_ID';`

    Estado local: `isDialogOpen: boolean`, `editingItem: Course | null`, `imageUrl: string`

    Zod schema (courseSchema):
    ```typescript
    const courseSchema = z.object({
      title:             z.string().min(3, 'Título obrigatório'),
      description:       z.string().optional(),
      date:              z.string().min(1, 'Data obrigatória'),
      modality:          z.string().optional(),
      location:          z.string().optional(),
      registration_link: z.string().url('URL inválida').or(z.literal('')).optional(),
    });
    type CourseFormData = z.infer<typeof courseSchema>;
    ```

    useForm com zodResolver(courseSchema). Reset form + setImageUrl ao abrir dialog.

    Mutations (com `queryClient.invalidateQueries({ queryKey: ['courses'] })` no onSuccess):
    - `createCourse`: insert com `{ ...formData, image_url: imageUrl || null }`
    - `updateCourse`: update com `{ ...formData, image_url: imageUrl || null }` where eq('id', editingItem.id)
    - `deleteCourse`: delete where eq('id', id) — window.confirm primeiro

    Formulário (Dialog) — campos em ordem:
    1. title (Input required, Label "Título")
    2. date (Input type="datetime-local" required, Label "Data e Hora")
    3. description (Textarea rows=3, Label "Descrição")
    4. modality (Select com opções "Presencial", "Online", "Híbrido", Label "Modalidade")
    5. location (Input, Label "Local / Link da Transmissão")
    6. registration_link (Input type="url", Label "Link de Inscrição")
    7. ImageUpload (siteId={OAB_MA_SITE_ID}, label="Imagem do Curso", value={imageUrl}, onChange={setImageUrl})

    Lista: grid `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
    Cada card mostra: imagem (thumbnail 80x80 object-cover ou ícone Calendar), título, data formatada (format do date-fns), modalidade, botões Edit/Delete com group-hover

    Exibir erros de validação do zod abaixo de cada campo com `text-destructive text-xs`.

    Estado loading/vazio igual SubsectionsEditor.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "CoursesEditor" || echo "sem erros TypeScript em CoursesEditor"</automated>
  </verify>
  <done>CoursesEditor.tsx compila sem erros TypeScript; implementa CRUD com validação zod e ImageUpload integrado</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar Cursos à sidebar e ao SiteEditor</name>
  <files>src/pages/admin/AdminLayout.tsx, src/pages/admin/SiteEditor.tsx</files>
  <action>
    Em src/pages/admin/AdminLayout.tsx:
    - Localizar o array `navItems` — agora já tem 'subsections' (adicionado no Plano 02)
    - Adicionar `{ id: 'courses', label: 'Cursos' }` APÓS `{ id: 'subsections', label: 'Subseções' }`
    - Não remover nem reordenar nenhum outro item

    Em src/pages/admin/SiteEditor.tsx:
    - Adicionar import: `import CoursesEditor from './components/CoursesEditor';`
    - Localizar o bloco de seção 'subsections' (adicionado no Plano 02)
    - Adicionar APÓS esse bloco:
      ```tsx
      {activeSection === 'courses' && (
        <CoursesEditor />
      )}
      ```
    - NÃO modificar nenhum outro bloco condicional existente
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "AdminLayout|SiteEditor" || echo "sem erros TypeScript"</automated>
  </verify>
  <done>AdminLayout.tsx tem 'courses' no navItems após 'subsections'; SiteEditor.tsx renderiza CoursesEditor quando activeSection === 'courses'</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verificar editor de cursos no painel admin</name>
  <what-built>CRUD de cursos com upload de imagem no painel admin</what-built>
  <how-to-verify>
    1. Execute `npm run dev` e acesse http://localhost:5173/admin (faça login)
    2. No menu lateral, confirme que "Cursos" aparece após "Subseções"
    3. Clique em "Cursos" — o CoursesEditor deve renderizar
    4. Teste criar curso: preencha título, data (data futura), modalidade, e upload de imagem — salve
    5. Confirme que o curso aparece na lista com thumbnail da imagem
    6. Teste criar curso passado (data no passado) — acesse /cursos e confirme que aparece na seção "Cursos Realizados" com opacidade reduzida
    7. Teste editar: clique no ícone de edição, modifique o título, salve — confirme que atualiza
    8. Teste excluir: confirme que o curso desaparece da lista e de /cursos
    9. Verifique validação: tente salvar sem título — confirme mensagem de erro abaixo do campo
    10. Verifique que "Subseções" na sidebar ainda funciona (sem regressão do Plano 02)
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passa sem erros nos arquivos modificados
- navItems em AdminLayout.tsx inclui 'courses' após 'subsections'
- SiteEditor.tsx tem bloco `activeSection === 'courses'`
- CoursesEditor usa react-hook-form + zod para validação
- ImageUpload integrado com OAB_MA_SITE_ID
</verification>

<success_criteria>
- Admin cria/edita/exclui cursos no painel com upload de imagem
- Cursos aparecem em /cursos imediatamente após criação
- Validação funciona (campo obrigatório mostra erro)
- TypeScript compila sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-public-site-content-management/02-04-SUMMARY.md`
</output>
