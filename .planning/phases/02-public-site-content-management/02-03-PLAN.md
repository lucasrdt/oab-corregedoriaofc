---
phase: 02-public-site-content-management
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Cursos.tsx
  - src/hooks/useCourses.ts
  - src/App.tsx
autonomous: true
requirements:
  - CUR-01
  - CUR-02
  - CUR-03

must_haves:
  truths:
    - "Visitante acessa /cursos e vê catálogo com imagem, título, descrição, data/hora, modalidade/local e link externo de inscrição"
    - "Cursos aparecem ordenados do mais próximo para o mais distante (order by date ASC)"
    - "Cursos passados são visivelmente distintos dos futuros (opacidade reduzida ou seção separada)"
    - "Enquanto os dados carregam, o usuário vê estado de loading"
    - "Se não houver cursos, a página exibe mensagem vazia em vez de quebrar"
  artifacts:
    - path: "src/pages/Cursos.tsx"
      provides: "Página pública /cursos com catálogo de cursos"
      exports: ["default Cursos"]
    - path: "src/hooks/useCourses.ts"
      provides: "react-query hook lendo tabela courses ordenada por date ASC"
      exports: ["useCourses", "Course"]
  key_links:
    - from: "src/pages/Cursos.tsx"
      to: "supabase.from('courses')"
      via: "useCourses hook"
      pattern: "useCourses"
    - from: "src/App.tsx"
      to: "src/pages/Cursos.tsx"
      via: "Route path=/cursos"
      pattern: "path.*cursos"
---

<objective>
Criar a página pública `/cursos` com catálogo de cursos dinâmico lido do Supabase, ordenado por data, com distinção visual entre cursos passados e futuros.

Purpose: CUR-01, CUR-02, CUR-03 — visitantes precisam ver os cursos e eventos disponíveis ordenados cronologicamente.
Output: Rota /cursos funcional, hook useCourses, página com distinção visual passado/futuro.
</objective>

<execution_context>
@C:/Users/gusta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-public-site-content-management/02-RESEARCH.md

<interfaces>
<!-- Interfaces críticas para o executor. -->

Supabase table schema (deve ser criada antes desta plan rodar):
```sql
-- Criar no Supabase Dashboard SQL Editor
CREATE TABLE IF NOT EXISTS public.courses (
  id                uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  title             text NOT NULL,
  description       text,
  date              timestamptz NOT NULL,
  modality          text,        -- 'Presencial' | 'Online' | 'Híbrido'
  location          text,
  registration_link text,
  image_url         text,
  created_at        timestamptz DEFAULT now(),
  updated_at        timestamptz DEFAULT now()
);
ALTER TABLE public.courses ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Public read" ON public.courses FOR SELECT USING (true);
CREATE POLICY "Auth write" ON public.courses FOR ALL USING (auth.role() = 'authenticated');
```

From date-fns v3 (já instalado):
```typescript
import { isPast, parseISO, format } from 'date-fns';
import { ptBR } from 'date-fns/locale';
// isPast(parseISO(course.date)) → true se curso já passou
// format(parseISO(course.date), "dd 'de' MMMM 'de' yyyy 'às' HH:mm", { locale: ptBR })
```

Route registration pattern (from App.tsx):
```typescript
// Adicionar com PageTransition, ANTES da rota catch-all /:slug
<Route path="/cursos" element={<PageTransition><Cursos /></PageTransition>} />
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 0: Criar tabela courses no Supabase Dashboard</name>
  <what-built>Nenhuma automação possível — tabela precisa existir antes do código funcionar</what-built>
  <how-to-verify>
    1. Acesse o Supabase Dashboard do projeto OAB-MA
    2. Vá em "SQL Editor" e execute:

    ```sql
    CREATE TABLE IF NOT EXISTS public.courses (
      id                uuid PRIMARY KEY DEFAULT gen_random_uuid(),
      title             text NOT NULL,
      description       text,
      date              timestamptz NOT NULL,
      modality          text,
      location          text,
      registration_link text,
      image_url         text,
      created_at        timestamptz DEFAULT now(),
      updated_at        timestamptz DEFAULT now()
    );
    ALTER TABLE public.courses ENABLE ROW LEVEL SECURITY;
    CREATE POLICY "Public read" ON public.courses FOR SELECT USING (true);
    CREATE POLICY "Auth write" ON public.courses FOR ALL USING (auth.role() = 'authenticated');
    ```

    3. Confirme que a tabela aparece em "Table Editor" com todas as colunas
    4. Opcionalmente insira 1 curso futuro e 1 curso passado para testar a distinção visual
  </how-to-verify>
  <resume-signal>Digite "tabela criada" após criar a tabela no Supabase Dashboard</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Hook useCourses e página Cursos.tsx</name>
  <files>src/hooks/useCourses.ts, src/pages/Cursos.tsx</files>
  <action>
    Criar src/hooks/useCourses.ts com:
    - Exportar tipo `Course`: `{ id: string; title: string; description?: string; date: string; modality?: string; location?: string; registration_link?: string; image_url?: string; created_at: string }`
    - `useCourses()` usando `useQuery` do @tanstack/react-query v5
    - queryKey: ['courses']
    - queryFn: `supabase.from('courses').select('*').order('date', { ascending: true })`
    - Lança erro se Supabase retornar erro

    Criar src/pages/Cursos.tsx com:
    - Importar `useCourses`, `Course` de `@/hooks/useCourses`
    - Importar `isPast`, `parseISO`, `format` de `date-fns` e `ptBR` de `date-fns/locale`
    - Estrutura: `<Header />`, conteúdo principal, `<Footer />`
    - Estado loading: grid com 4 skeleton cards `animate-pulse bg-muted rounded-xl h-64`
    - Estado erro: "Erro ao carregar cursos. Tente novamente mais tarde."
    - Estado vazio: "Nenhum curso disponível no momento."
    - Estado preenchido: separar em dois grupos usando `isPast(parseISO(course.date))`:
      - Grupo "Próximos Cursos" (past=false) — exibir normalmente
      - Grupo "Cursos Realizados" (past=true) — exibir com `opacity-50` e `grayscale` aplicados ao card
      - Se grupo vazio, não exibir o cabeçalho desse grupo
    - Cada card (shadcn Card):
      - Imagem: se `image_url` existir, `<img>` com aspect-video e object-cover; se não, placeholder cinza com ícone BookOpen do lucide-react
      - Título: `text-lg font-semibold`
      - Descrição: `text-sm text-muted-foreground line-clamp-3`
      - Data formatada: `format(parseISO(course.date), "dd 'de' MMMM 'de' yyyy 'às' HH:mm", { locale: ptBR })`
      - Modalidade e local: ícone MapPin + texto, omitir se vazio
      - Link de inscrição: se `registration_link` existir, `<a href={...} target="_blank" rel="noopener noreferrer">` com aparência de botão (variant="outline" size="sm"), label "Inscrever-se"
    - Layout: grid `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "Cursos|useCourses" || echo "sem erros TypeScript nesses arquivos"</automated>
  </verify>
  <done>src/hooks/useCourses.ts e src/pages/Cursos.tsx compilam sem erros; distinção passado/futuro implementada com isPast + grupos separados</done>
</task>

<task type="auto">
  <name>Task 2: Registrar rota /cursos no App.tsx</name>
  <files>src/App.tsx</files>
  <action>
    Em src/App.tsx:
    - Adicionar import: `import Cursos from "./pages/Cursos";`
    - Adicionar rota nas rotas públicas: `<Route path="/cursos" element={<PageTransition><Cursos /></PageTransition>} />`
    - Posicionar ANTES da rota catch-all `/:slug` (linha ~59 do arquivo original)
    - Não remover nem modificar nenhuma rota existente
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "App.tsx" || echo "sem erros TypeScript em App.tsx"</automated>
  </verify>
  <done>Rota /cursos registrada no App.tsx com PageTransition, posicionada antes de /:slug</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verificar página /cursos</name>
  <what-built>Página pública /cursos com catálogo ordenado e distinção visual passado/futuro</what-built>
  <how-to-verify>
    1. Execute `npm run dev` e acesse http://localhost:5173/cursos
    2. Se inseriu dados de teste (1 passado + 1 futuro no Task 0):
       - Confirme que o curso futuro aparece na seção "Próximos Cursos" em cores normais
       - Confirme que o curso passado aparece na seção "Cursos Realizados" com opacidade reduzida/escala de cinza
       - Confirme que a data está formatada em PT-BR (ex: "15 de abril de 2026 às 14:00")
    3. Se tabela vazia: confirme mensagem "Nenhum curso disponível no momento."
    4. Confirme que o link de inscrição (se preenchido) abre em nova aba
    5. Confirme que a rota /subsecoes (Plano 01) continua funcionando — sem regressão no App.tsx
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passa sem erros nos arquivos modificados
- Rota /cursos existe no App.tsx antes de /:slug
- Hook useCourses usa `order('date', { ascending: true })` — CUR-02
- Cursos passados renderizados com `opacity-50 grayscale` — CUR-03
</verification>

<success_criteria>
- Visitante acessa /cursos e vê catálogo ou mensagem vazia (não quebra)
- Cursos ordenados por data crescente
- Cursos passados visualmente distintos dos futuros
- TypeScript compila sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-public-site-content-management/02-03-SUMMARY.md`
</output>
