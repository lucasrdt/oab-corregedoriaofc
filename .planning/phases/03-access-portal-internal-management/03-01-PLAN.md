---
phase: 03-access-portal-internal-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260318_phase3_roles_casos.sql
  - supabase/functions/admin-create-user/index.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - CASO-01
  - CASO-02
  - CASO-03
  - CASO-04
  - CASO-05
  - UPL-02

must_haves:
  truths:
    - "A tabela `user_roles` existe no Supabase com colunas user_id (uuid), role (text), subsection_id (uuid nullable)"
    - "RLS está habilitado em `user_roles`: usuário vê apenas seu próprio registro, admin vê todos"
    - "A tabela `casos` existe com subsection_id, campos de processo e documentos JSONB"
    - "RLS está habilitado em `casos`: admin vê tudo, presidente vê/edita apenas sua subseção, user tem SELECT apenas na sua subseção"
    - "Edge Function `admin-create-user` existe e rejeita chamadas de não-admins com 403"
    - "Policies de Storage permitem authenticated users fazer upload e leitura em `casos/` no bucket `site-assets`"
  artifacts:
    - path: "supabase/migrations/20260318_phase3_roles_casos.sql"
      provides: "Criação das tabelas user_roles e casos com RLS policies completas"
      contains: "CREATE TABLE user_roles, CREATE TABLE casos, ENABLE ROW LEVEL SECURITY"
    - path: "supabase/functions/admin-create-user/index.ts"
      provides: "Edge Function Deno para criar usuários via service_role com segurança"
      exports: ["serve handler"]
  key_links:
    - from: "supabase/migrations/20260318_phase3_roles_casos.sql"
      to: "auth.users"
      via: "user_roles.user_id REFERENCES auth.users(id)"
      pattern: "REFERENCES auth\\.users"
    - from: "supabase/functions/admin-create-user/index.ts"
      to: "user_roles"
      via: "supabaseAdmin.from('user_roles').insert()"
      pattern: "user_roles.*insert"
---

<objective>
Criar a fundação de dados da Fase 3: tabelas `user_roles` e `casos` no Supabase com RLS policies completas, e a Edge Function para criação de usuários pelo admin.

Purpose: Toda a segurança do portal é aplicada na camada de banco de dados via RLS — não no frontend. Sem essa fundação, nenhuma outra parte da Fase 3 pode funcionar com segurança real.
Output: Migration SQL aplicável ao Supabase, Edge Function Deno deployável.
</objective>

<execution_context>
@C:/Users/gusta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-access-portal-internal-management/03-RESEARCH.md

<interfaces>
<!-- Tabela subsecoes esperada da Fase 2 — caso não exista ainda, usar subsection_id como text -->
<!-- Por segurança, a FK para subsecoes é adicionada como DEFERRABLE ou simplesmente como uuid sem FK durante esta fase -->

De src/pages/admin/components/DocumentUploadEditor.tsx:
```typescript
interface DocumentUploadEditorProps {
    title: string;
    documents: Document[];
    onChange: (docs: Document[]) => void;
    bucket?: string;  // default: "site-assets"
    siteId: string;
}
```

De src/lib/supabase.ts:
```typescript
export const supabase = createClient(supabaseUrl, supabaseAnonKey);
// Bucket usado: "site-assets"
// Path convention existente: {siteId}/{timestamp}_{filename}
// Novo convention para casos: casos/{caso_id}/{timestamp}_{filename}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration SQL para user_roles e casos com RLS</name>
  <files>supabase/migrations/20260318_phase3_roles_casos.sql</files>
  <action>
Criar o arquivo de migration com todo o SQL necessário para as duas tabelas e suas RLS policies.

IMPORTANTE — evitar recursão infinita em user_roles: A policy de admin em `user_roles` NÃO pode fazer SELECT em `user_roles` diretamente (causa infinite recursion). Usar uma função SECURITY DEFINER `is_admin()` para quebrar o ciclo.

Conteúdo do arquivo:

```sql
-- ============================================================
-- Phase 3: user_roles + casos tables with RLS
-- ============================================================

-- Helper function: verifica se o usuário atual é admin
-- SECURITY DEFINER: roda sem RLS, evita recursão infinita
CREATE OR REPLACE FUNCTION is_admin()
RETURNS boolean
LANGUAGE sql SECURITY DEFINER STABLE
AS $$
  SELECT EXISTS (
    SELECT 1 FROM user_roles
    WHERE user_id = auth.uid() AND role = 'admin'
  );
$$;

-- Helper function: retorna subsection_id do usuário atual
CREATE OR REPLACE FUNCTION get_user_subsection()
RETURNS uuid
LANGUAGE sql SECURITY DEFINER STABLE
AS $$
  SELECT subsection_id FROM user_roles
  WHERE user_id = auth.uid()
  LIMIT 1;
$$;

-- ============================================================
-- Tabela: user_roles
-- ============================================================
CREATE TABLE IF NOT EXISTS user_roles (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id       uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  role          text NOT NULL CHECK (role IN ('admin', 'dev', 'presidente', 'user')),
  subsection_id uuid,  -- FK para subsecoes será adicionada na Fase 2 se tabela existir
  active        boolean NOT NULL DEFAULT true,
  created_at    timestamptz NOT NULL DEFAULT now(),
  updated_at    timestamptz NOT NULL DEFAULT now(),
  UNIQUE (user_id)
);

ALTER TABLE user_roles ENABLE ROW LEVEL SECURITY;

-- Usuário lê seu próprio registro
CREATE POLICY "user_roles_select_own"
  ON user_roles FOR SELECT
  USING (auth.uid() = user_id);

-- Admin lê todos os registros (usa função SECURITY DEFINER para evitar recursão)
CREATE POLICY "user_roles_select_admin"
  ON user_roles FOR SELECT
  USING (is_admin());

-- Somente admin pode inserir/atualizar/deletar
CREATE POLICY "user_roles_write_admin"
  ON user_roles FOR ALL
  USING (is_admin())
  WITH CHECK (is_admin());

-- ============================================================
-- Tabela: casos
-- ============================================================
CREATE TABLE IF NOT EXISTS casos (
  id                 uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  subsection_id      uuid NOT NULL,  -- ref a subsecoes(id) — FK adicionada após Fase 2
  nome               text NOT NULL,
  processo           text NOT NULL,
  comarca            text,
  uf                 char(2),
  vara               text,
  especialista       text,
  passivo            text,
  credores           integer DEFAULT 0,
  ajuizamento        date,
  deferimento        date,
  link_habilitacoes  text,
  documentos         jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at         timestamptz NOT NULL DEFAULT now(),
  updated_at         timestamptz NOT NULL DEFAULT now()
);

ALTER TABLE casos ENABLE ROW LEVEL SECURITY;

-- Admin: acesso total a todos os casos
CREATE POLICY "casos_admin_all"
  ON casos FOR ALL
  USING (is_admin())
  WITH CHECK (is_admin());

-- Presidente: acesso total apenas aos casos da sua subseção
CREATE POLICY "casos_presidente_all"
  ON casos FOR ALL
  USING (
    EXISTS (
      SELECT 1 FROM user_roles
      WHERE user_id = auth.uid()
        AND role = 'presidente'
        AND subsection_id = casos.subsection_id
        AND active = true
    )
  )
  WITH CHECK (
    EXISTS (
      SELECT 1 FROM user_roles
      WHERE user_id = auth.uid()
        AND role = 'presidente'
        AND subsection_id = casos.subsection_id
        AND active = true
    )
  );

-- User: somente leitura na sua subseção
CREATE POLICY "casos_user_select"
  ON casos FOR SELECT
  USING (
    EXISTS (
      SELECT 1 FROM user_roles
      WHERE user_id = auth.uid()
        AND role = 'user'
        AND subsection_id = casos.subsection_id
        AND active = true
    )
  );

-- ============================================================
-- Storage policies: bucket site-assets, path casos/*
-- ============================================================
-- Executar no dashboard Supabase (Storage > Policies) ou via SQL editor:

CREATE POLICY "authenticated_upload_casos"
  ON storage.objects FOR INSERT
  TO authenticated
  WITH CHECK (bucket_id = 'site-assets' AND name LIKE 'casos/%');

CREATE POLICY "authenticated_read_casos"
  ON storage.objects FOR SELECT
  TO authenticated
  USING (bucket_id = 'site-assets' AND name LIKE 'casos/%');

CREATE POLICY "authenticated_delete_casos"
  ON storage.objects FOR DELETE
  TO authenticated
  USING (bucket_id = 'site-assets' AND name LIKE 'casos/%');
```

Após criar o arquivo, aplicar no Supabase:
1. Via Supabase CLI: `supabase db push` (se CLI estiver configurado)
2. OU copiar o conteúdo do arquivo e executar no SQL Editor do dashboard Supabase (Project > SQL Editor > New query)

Registrar no comentário do SUMMARY qual método foi usado.
  </action>
  <verify>
    <automated>
      node -e "const fs = require('fs'); const sql = fs.readFileSync('supabase/migrations/20260318_phase3_roles_casos.sql', 'utf8'); const checks = ['CREATE TABLE IF NOT EXISTS user_roles', 'CREATE TABLE IF NOT EXISTS casos', 'ENABLE ROW LEVEL SECURITY', 'SECURITY DEFINER', 'casos_admin_all', 'casos_presidente_all', 'casos_user_select', 'authenticated_upload_casos']; checks.forEach(c => { if (!sql.includes(c)) throw new Error('Missing: ' + c); }); console.log('SQL migration valid — all required sections present');"
    </automated>
  </verify>
  <done>Arquivo SQL existe com tabelas user_roles e casos, RLS habilitado em ambas, 5+ policies criadas incluindo função SECURITY DEFINER is_admin(), e policies de Storage para path casos/*</done>
</task>

<task type="auto">
  <name>Task 2: Criar Edge Function admin-create-user</name>
  <files>supabase/functions/admin-create-user/index.ts</files>
  <action>
Criar a Edge Function Deno que permite ao admin criar usuários no portal. Esta função é necessária porque `supabase.auth.admin.createUser()` requer a service_role key, que nunca deve estar no frontend.

A função deve:
1. Verificar o token Bearer do caller — extrair user_id do JWT
2. Buscar role do caller em user_roles — rejeitar com 403 se não for admin
3. Criar o usuário em auth.users via supabaseAdmin.auth.admin.createUser()
4. Inserir linha em user_roles com o role e subsection_id escolhidos
5. Retornar { user } em caso de sucesso, { error } em caso de falha

Criar a pasta `supabase/functions/admin-create-user/` se não existir.

```typescript
// supabase/functions/admin-create-user/index.ts
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts';
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
};

serve(async (req: Request) => {
  // Handle CORS preflight
  if (req.method === 'OPTIONS') {
    return new Response('ok', { headers: corsHeaders });
  }

  try {
    const supabaseAdmin = createClient(
      Deno.env.get('SUPABASE_URL')!,
      Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!,
      { auth: { autoRefreshToken: false, persistSession: false } }
    );

    // Verificar que o caller é admin
    const authHeader = req.headers.get('Authorization');
    if (!authHeader) {
      return new Response(JSON.stringify({ error: 'Missing authorization header' }), {
        status: 401, headers: { ...corsHeaders, 'Content-Type': 'application/json' }
      });
    }

    const token = authHeader.replace('Bearer ', '');
    const { data: { user: caller }, error: userError } = await supabaseAdmin.auth.getUser(token);

    if (userError || !caller) {
      return new Response(JSON.stringify({ error: 'Invalid token' }), {
        status: 401, headers: { ...corsHeaders, 'Content-Type': 'application/json' }
      });
    }

    const { data: callerRole } = await supabaseAdmin
      .from('user_roles')
      .select('role')
      .eq('user_id', caller.id)
      .eq('active', true)
      .single();

    if (callerRole?.role !== 'admin') {
      return new Response(JSON.stringify({ error: 'Forbidden: admin only' }), {
        status: 403, headers: { ...corsHeaders, 'Content-Type': 'application/json' }
      });
    }

    // Criar o novo usuário
    const body = await req.json();
    const { email, password, role, subsection_id } = body;

    if (!email || !password || !role) {
      return new Response(JSON.stringify({ error: 'Missing required fields: email, password, role' }), {
        status: 400, headers: { ...corsHeaders, 'Content-Type': 'application/json' }
      });
    }

    const validRoles = ['admin', 'dev', 'presidente', 'user'];
    if (!validRoles.includes(role)) {
      return new Response(JSON.stringify({ error: `Invalid role. Must be one of: ${validRoles.join(', ')}` }), {
        status: 400, headers: { ...corsHeaders, 'Content-Type': 'application/json' }
      });
    }

    const { data: { user: newUser }, error: createError } = await supabaseAdmin.auth.admin.createUser({
      email,
      password,
      email_confirm: true,
    });

    if (createError || !newUser) {
      return new Response(JSON.stringify({ error: createError?.message || 'Failed to create user' }), {
        status: 400, headers: { ...corsHeaders, 'Content-Type': 'application/json' }
      });
    }

    // Inserir registro em user_roles atomicamente
    const { error: roleError } = await supabaseAdmin.from('user_roles').insert({
      user_id: newUser.id,
      role,
      subsection_id: subsection_id || null,
      active: true,
    });

    if (roleError) {
      // Rollback: remover usuário criado se inserção de role falhar
      await supabaseAdmin.auth.admin.deleteUser(newUser.id);
      return new Response(JSON.stringify({ error: 'Failed to assign role: ' + roleError.message }), {
        status: 500, headers: { ...corsHeaders, 'Content-Type': 'application/json' }
      });
    }

    return new Response(JSON.stringify({ user: { id: newUser.id, email: newUser.email } }), {
      status: 200, headers: { ...corsHeaders, 'Content-Type': 'application/json' }
    });

  } catch (err: any) {
    return new Response(JSON.stringify({ error: err.message }), {
      status: 500, headers: { ...corsHeaders, 'Content-Type': 'application/json' }
    });
  }
});
```

Para deploy da Edge Function:
- Via CLI: `supabase functions deploy admin-create-user` (requer Supabase CLI autenticado)
- OU via dashboard: Project > Edge Functions > Deploy new function

Registrar no SUMMARY qual método foi usado.
  </action>
  <verify>
    <automated>
      node -e "const fs = require('fs'); const fn = fs.readFileSync('supabase/functions/admin-create-user/index.ts', 'utf8'); const checks = ['SUPABASE_SERVICE_ROLE_KEY', 'auth.admin.createUser', 'user_roles.*insert|insert.*user_roles', 'role.*admin', 'Forbidden', 'email_confirm: true']; ['SUPABASE_SERVICE_ROLE_KEY', 'auth.admin.createUser', 'role.*!==.*admin|role.*!=.*admin', 'Forbidden', 'email_confirm'].forEach(c => { const r = new RegExp(c); if (!r.test(fn)) throw new Error('Missing pattern: ' + c); }); console.log('Edge Function valid — all security checks present');"
    </automated>
  </verify>
  <done>Arquivo existe com: verificação de token do caller, check de role admin via user_roles, criação do usuário via supabaseAdmin.auth.admin.createUser, inserção atômica em user_roles com rollback em caso de falha, CORS headers</done>
</task>

</tasks>

<verification>
Executar validações:
1. `node -e "require('fs').readFileSync('supabase/migrations/20260318_phase3_roles_casos.sql', 'utf8')" && echo "migration exists"`
2. `node -e "require('fs').readFileSync('supabase/functions/admin-create-user/index.ts', 'utf8')" && echo "edge function exists"`
3. Verificar que a migration SQL contém `is_admin()` como função SECURITY DEFINER (anti-recursão)
4. Verificar que a Edge Function nunca expõe SUPABASE_SERVICE_ROLE_KEY em responses
</verification>

<success_criteria>
- Arquivo `supabase/migrations/20260318_phase3_roles_casos.sql` existe com tabelas user_roles e casos, RLS habilitado, função is_admin() SECURITY DEFINER, policies completas para todos os roles
- Arquivo `supabase/functions/admin-create-user/index.ts` existe com verificação de admin, criação via service_role, e rollback em falha
- Nenhuma service_role key está hardcoded — tudo via Deno.env.get()
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-access-portal-internal-management/03-01-SUMMARY.md`
</output>
