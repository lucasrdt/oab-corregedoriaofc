---
phase: 03-access-portal-internal-management
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/pages/portal/PortalLayout.tsx
  - src/pages/portal/AdminDashboard.tsx
  - src/pages/portal/DevDashboard.tsx
  - src/pages/portal/PresidenteDashboard.tsx
  - src/pages/portal/UserDashboard.tsx
  - src/App.tsx
autonomous: false
requirements:
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUTH-08
  - AUTH-09

must_haves:
  truths:
    - "Cada role tem um dashboard dedicado acessível após login"
    - "Admin vê sidebar com: Usuários, Subseções (todas), Casos (todos) — sem escopo restrito"
    - "Dev vê sidebar com: Configurações, Conteúdo Global — sem acesso a casos"
    - "Presidente vê sidebar com: Casos da Minha Subseção — sem ver outras subseções"
    - "User vê sidebar com: Meus Casos (somente leitura) — sem ver outras subseções"
    - "Admin consegue criar usuário via formulário que chama a Edge Function admin-create-user"
    - "Tentar acessar /portal/admin como presidente redireciona para /portal/presidente"
  artifacts:
    - path: "src/pages/portal/PortalLayout.tsx"
      provides: "Layout base com sidebar para todos os dashboards do portal"
      exports: ["PortalLayout (default)"]
    - path: "src/pages/portal/AdminDashboard.tsx"
      provides: "Dashboard do admin com gestão de usuários"
      exports: ["AdminDashboard (default)"]
    - path: "src/pages/portal/DevDashboard.tsx"
      provides: "Dashboard do dev com acesso a conteúdo global"
      exports: ["DevDashboard (default)"]
    - path: "src/pages/portal/PresidenteDashboard.tsx"
      provides: "Dashboard do presidente com lista de casos da sua subseção"
      exports: ["PresidenteDashboard (default)"]
    - path: "src/pages/portal/UserDashboard.tsx"
      provides: "Dashboard do user com lista somente leitura dos casos da subseção"
      exports: ["UserDashboard (default)"]
  key_links:
    - from: "src/pages/portal/AdminDashboard.tsx"
      to: "supabase/functions/admin-create-user"
      via: "supabase.functions.invoke('admin-create-user', { body: { email, password, role, subsection_id } })"
      pattern: "functions.invoke.*admin-create-user"
    - from: "src/pages/portal/PresidenteDashboard.tsx"
      to: "supabase (tabela casos)"
      via: "supabase.from('casos').select('*') — RLS filtra automaticamente por subsection_id"
      pattern: "from.*casos"
    - from: "src/pages/portal/PortalLayout.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuth() para exibir sidebar correta por role e botão de logout"
      pattern: "useAuth"
---

<objective>
Construir os quatro dashboards de role (admin, dev, presidente, user) com PortalLayout sidebar, e a gestão de usuários no AdminDashboard via Edge Function.

Purpose: Sem os dashboards, o fluxo de login e redirecionamento do plano 03-02 não tem destino funcional.
Output: Cinco componentes React (PortalLayout + 4 dashboards); AdminDashboard com CRUD de usuários; App.tsx atualizado com componentes reais substituindo placeholders.
</objective>

<execution_context>
@C:/Users/gusta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-access-portal-internal-management/03-RESEARCH.md
@.planning/phases/03-access-portal-internal-management/03-02-SUMMARY.md

<interfaces>
<!-- useAuth hook (de 03-02) -->
```typescript
import { useAuth } from '@/contexts/AuthContext';
const { session, role, subsectionId, loading, signOut } = useAuth();
// role: 'admin' | 'dev' | 'presidente' | 'user' | null
// subsectionId: string | null (uuid da subseção do presidente/user)
```

<!-- PortalLayout props contract (novo) -->
```typescript
interface PortalLayoutProps {
  children: React.ReactNode;
  title: string;           // Título da página exibido no header
  navItems: { id: string; label: string; icon?: LucideIcon }[];
  activeItem?: string;
  onNavClick?: (id: string) => void;
}
```

<!-- AdminLayout existente (para referência de padrão visual) -->
// src/pages/admin/AdminLayout.tsx usa SidebarProvider, Sidebar, SidebarContent, SidebarHeader, SidebarInset
// PortalLayout deve seguir o mesmo padrão visual com shadcn/ui sidebar

<!-- Edge Function invocation -->
```typescript
// Chamar de AdminDashboard:
const { data, error } = await supabase.functions.invoke('admin-create-user', {
  body: { email, password, role, subsection_id: selectedSubsectionId || null },
});
```

<!-- Buscar casos (RLS filtra automaticamente) -->
```typescript
const { data: casos } = await supabase
  .from('casos')
  .select('id, nome, processo, comarca, subsection_id, created_at')
  .order('created_at', { ascending: false });
// Presidente recebe apenas casos de sua subseção
// Admin recebe todos os casos
```

<!-- Buscar usuários para o admin -->
```typescript
const { data: users } = await supabase
  .from('user_roles')
  .select('id, user_id, role, subsection_id, active, created_at');
// RLS: admin vê todos via policy user_roles_select_admin
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar PortalLayout e DevDashboard/UserDashboard</name>
  <files>src/pages/portal/PortalLayout.tsx, src/pages/portal/DevDashboard.tsx, src/pages/portal/UserDashboard.tsx</files>
  <action>
**src/pages/portal/PortalLayout.tsx:**
Adaptar o padrão do AdminLayout (SidebarProvider, Sidebar, SidebarInset) para o portal. Diferenças:
- Header inclui: nome do usuário logado (session.user.email) + botão "Sair" que chama signOut() e navega para /portal
- Badge de role no header (ex: "ADMIN", "PRESIDENTE") com cor diferente por role (usar Badge do shadcn/ui)
- Logo/título no topo da sidebar: "Portal OAB-MA" com ícone Shield de lucide-react
- Sidebar fecha no mobile ao clicar em nav item (mesmo padrão do AdminLayout: isMobile + setOpenMobile(false))
- Props: children, title (string para o header da página), navItems array, activeItem, onNavClick

**src/pages/portal/DevDashboard.tsx:**
Dashboard para role `dev`. Nav items: [{ id: 'site', label: 'Configurações do Site' }, { id: 'conteudo', label: 'Conteúdo Global' }].

Conteúdo da tela:
- Card com título "Painel de Desenvolvimento" e descrição "Acesso a configurações e conteúdo global do site"
- Botão que navega para `/admin/editor` (o painel admin existente, acessível ao dev)
- Nota informativa: "Acesse o painel de conteúdo completo via botão abaixo. Casos de subseções não são visíveis a este role."
- Usar PortalLayout como wrapper

**src/pages/portal/UserDashboard.tsx:**
Dashboard para role `user` (somente leitura). Nav items: [{ id: 'casos', label: 'Casos da Subseção' }].

Conteúdo:
- Buscar casos via `supabase.from('casos').select(...)` — RLS retorna apenas da subseção do usuário
- Exibir lista de casos em Cards: nome, processo, comarca, UF, data criação
- Estado de loading com spinner
- Estado vazio: "Nenhum caso encontrado para sua subseção"
- Sem botões de editar/criar — somente leitura
- Usar @tanstack/react-query (useQuery) para a busca
  </action>
  <verify>
    <automated>
      node -e "const fs = require('fs'); ['src/pages/portal/PortalLayout.tsx', 'src/pages/portal/DevDashboard.tsx', 'src/pages/portal/UserDashboard.tsx'].forEach(f => { const c = fs.readFileSync(f, 'utf8'); console.log(f + ': ' + c.length + ' chars'); }); const pl = fs.readFileSync('src/pages/portal/PortalLayout.tsx', 'utf8'); ['signOut', 'SidebarProvider', 'setOpenMobile', 'session'].forEach(k => { if (!pl.includes(k)) throw new Error('PortalLayout missing: ' + k); }); const ud = fs.readFileSync('src/pages/portal/UserDashboard.tsx', 'utf8'); if (!ud.includes('casos')) throw new Error('UserDashboard missing casos query'); console.log('All 3 files valid');"
    </automated>
  </verify>
  <done>PortalLayout com header de role badge + signOut, sidebar mobile-friendly. DevDashboard com link para /admin/editor. UserDashboard com lista somente-leitura de casos via useQuery.</done>
</task>

<task type="auto">
  <name>Task 2: Criar AdminDashboard (gestão de usuários) e PresidenteDashboard, atualizar App.tsx</name>
  <files>src/pages/portal/AdminDashboard.tsx, src/pages/portal/PresidenteDashboard.tsx, src/App.tsx</files>
  <action>
**src/pages/portal/AdminDashboard.tsx:**
Dashboard do admin. Nav items: [{ id: 'usuarios', label: 'Usuários' }, { id: 'casos', label: 'Todos os Casos' }, { id: 'site', label: 'Configurações' }].

Seção "Usuários" (activeItem === 'usuarios' por padrão):
- Tabela/lista de todos os usuários com colunas: email (usar user_id como ID), role, subsection_id, active
  - Nota: user_roles não tem o email — exibir user_id truncado como identificador. Admin verá o email ao criar o usuário.
- Formulário de criação de usuário (Dialog do shadcn/ui, trigger: botão "Novo Usuário"):
  - Campos: email (Input), senha (Input type=password), role (Select: admin/dev/presidente/user), subseção (Input UUID — opcional, só aparece se role = presidente ou user)
  - Ao submeter: chamar `supabase.functions.invoke('admin-create-user', { body: { email, password, role, subsection_id } })`
  - Sucesso: toast "Usuário criado com sucesso!" + fechar Dialog + refetch da lista
  - Erro: toast com mensagem de erro da Edge Function
- Ação de desativar usuário: botão toggle na lista que atualiza `user_roles.active = false` via `supabase.from('user_roles').update({ active: false }).eq('id', roleId)`
- Usar react-hook-form + zod para o formulário de criação

Seção "Todos os Casos" (activeItem === 'casos'):
- Lista simples de todos os casos (RLS retorna tudo para admin)
- Colunas: nome, processo, comarca, subseção (UUID truncado), created_at
- Cada linha tem link "Ver/Editar" que navega para `/portal/presidente/casos/{id}` (o editor de casos do plano 03-04)

Seção "Configurações" (activeItem === 'site'):
- Card com botão que navega para `/admin/editor`

**src/pages/portal/PresidenteDashboard.tsx:**
Dashboard do presidente. Nav items: [{ id: 'casos', label: 'Casos da Subseção' }].

Conteúdo:
- Buscar casos via useQuery: `supabase.from('casos').select('*').order('created_at', { ascending: false })`
- RLS filtra automaticamente para a subseção do presidente logado
- Exibir lista de casos em Cards com: nome, processo, comarca, UF, data ajuizamento
- Botão "Novo Caso" → navega para `/portal/presidente/casos/new`
- Cada card tem botão "Editar" → navega para `/portal/presidente/casos/{id}`
- Estado vazio: "Nenhum caso ainda. Clique em 'Novo Caso' para adicionar."
- Usar PortalLayout

**src/App.tsx:**
Substituir os placeholders `<div>... Em breve</div>` adicionados em 03-02 pelos componentes reais.
Adicionar também a rota do editor de casos do presidente (a ser implementada em 03-04, por enquanto criar com placeholder):

```tsx
// Imports a adicionar:
import AdminDashboard from './pages/portal/AdminDashboard';
import DevDashboard from './pages/portal/DevDashboard';
import PresidenteDashboard from './pages/portal/PresidenteDashboard';
import UserDashboard from './pages/portal/UserDashboard';

// Substituir placeholders:
<Route path="/portal/admin" element={<AdminDashboard />} />
<Route path="/portal/dev" element={<DevDashboard />} />
<Route path="/portal/presidente" element={<PresidenteDashboard />} />
<Route path="/portal/presidente/casos/:casoId" element={<div>Editor de Caso — Plano 03-04</div>} />
<Route path="/portal/user" element={<UserDashboard />} />
```
  </action>
  <verify>
    <automated>
      node -e "const fs = require('fs'); ['src/pages/portal/AdminDashboard.tsx', 'src/pages/portal/PresidenteDashboard.tsx'].forEach(f => { const c = fs.readFileSync(f, 'utf8'); console.log(f + ': ' + c.length + ' chars'); }); const ad = fs.readFileSync('src/pages/portal/AdminDashboard.tsx', 'utf8'); ['admin-create-user', 'functions.invoke', 'react-hook-form', 'active'].forEach(k => { if (!ad.includes(k)) throw new Error('AdminDashboard missing: ' + k); }); const pd = fs.readFileSync('src/pages/portal/PresidenteDashboard.tsx', 'utf8'); ['casos', 'Novo Caso', 'useQuery'].forEach(k => { if (!pd.includes(k)) throw new Error('PresidenteDashboard missing: ' + k); }); const app = fs.readFileSync('src/App.tsx', 'utf8'); ['AdminDashboard', 'PresidenteDashboard', 'UserDashboard', 'DevDashboard', 'casos/:casoId'].forEach(k => { if (!app.includes(k)) throw new Error('App.tsx missing: ' + k); }); console.log('All files valid');"
    </automated>
  </verify>
  <done>AdminDashboard com criação de usuário via Edge Function e desativação. PresidenteDashboard com lista de casos filtrada por RLS + navegação para editor. App.tsx com todos os dashboards reais.</done>
</task>

</tasks>

<verification>
Verificação funcional — executar `npm run dev`:
1. Login como admin → /portal/admin → seção Usuários mostra lista + Dialog de novo usuário
2. Formulário de novo usuário com role=presidente exibe campo subsection_id
3. Login como presidente → /portal/presidente → lista de casos da subseção (pode estar vazia)
4. Login como user → /portal/user → lista somente leitura
5. Login como dev → /portal/dev → card com link para /admin/editor
</verification>

<success_criteria>
- 5 arquivos criados: PortalLayout.tsx, AdminDashboard.tsx, DevDashboard.tsx, PresidenteDashboard.tsx, UserDashboard.tsx
- AdminDashboard chama supabase.functions.invoke('admin-create-user') para criar usuários
- Todos os dashboards usam PortalLayout com sidebar e badge de role
- App.tsx tem componentes reais (não placeholders) para todos os routes /portal/*
- `npm run build` sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-access-portal-internal-management/03-03-SUMMARY.md`
</output>
