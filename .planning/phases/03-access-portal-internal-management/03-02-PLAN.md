---
phase: 03-access-portal-internal-management
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/contexts/AuthContext.tsx
  - src/components/PortalRoute.tsx
  - src/pages/portal/PortalLogin.tsx
  - src/App.tsx
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUTH-08

must_haves:
  truths:
    - "Existe rota `/portal` com página de login pública (não protegida)"
    - "Após login bem-sucedido, o usuário é redirecionado ao dashboard correto: /portal/admin, /portal/dev, /portal/presidente ou /portal/user"
    - "AuthContext provê session, role, subsectionId e loading para toda a árvore do portal"
    - "PortalRoute redireciona não-autenticados para /portal, e autenticados com role errado para seu dashboard correto"
    - "Enquanto role está sendo carregado (loading=true), mostra spinner — não redireciona prematuramente"
    - "Rotas /admin/* existentes continuam funcionando sem quebrar"
  artifacts:
    - path: "src/contexts/AuthContext.tsx"
      provides: "Context com session + role fetch de user_roles, hook useAuth()"
      exports: ["AuthProvider", "useAuth"]
    - path: "src/components/PortalRoute.tsx"
      provides: "Guard de rota com verificação de role"
      exports: ["PortalRoute"]
    - path: "src/pages/portal/PortalLogin.tsx"
      provides: "Página de login pública em /portal"
      exports: ["PortalLogin (default)"]
    - path: "src/App.tsx"
      provides: "Rotas do portal adicionadas sem quebrar rotas existentes"
      contains: "path=\"/portal\""
  key_links:
    - from: "src/pages/portal/PortalLogin.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "supabase.auth.signInWithPassword + redirect para ROLE_DASHBOARDS[role]"
      pattern: "ROLE_DASHBOARDS"
    - from: "src/components/PortalRoute.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuth() — verifica session e role antes de renderizar Outlet"
      pattern: "useAuth"
    - from: "src/App.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "AuthProvider wrapping portal routes only (inside BrowserRouter)"
      pattern: "AuthProvider"
---

<objective>
Construir a infraestrutura de autenticação React do portal: AuthContext, PortalRoute guard, página de login em /portal, e integração das novas rotas em App.tsx sem quebrar as rotas /admin/* existentes.

Purpose: Sem AuthContext e PortalRoute, nenhum dashboard pode ser construído com segurança — eles dependem de useAuth() para saber qual role está logado.
Output: Sistema de auth completo do lado frontend; /portal funciona para login e redireciona corretamente por role.
</objective>

<execution_context>
@C:/Users/gusta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-access-portal-internal-management/03-RESEARCH.md
@.planning/phases/03-access-portal-internal-management/03-01-SUMMARY.md

<interfaces>
<!-- AuthContext contract (novo) -->
```typescript
// src/contexts/AuthContext.tsx
type Role = 'admin' | 'dev' | 'presidente' | 'user' | null;

interface AuthContextValue {
  session: Session | null;  // @supabase/supabase-js Session type
  role: Role;
  subsectionId: string | null;
  loading: boolean;
  signOut: () => Promise<void>;
}

export const AuthProvider: React.FC<{ children: React.ReactNode }>;
export const useAuth: () => AuthContextValue;
```

<!-- PortalRoute contract (novo) -->
```typescript
// src/components/PortalRoute.tsx
interface PortalRouteProps {
  allowedRoles: Role[];
}
export const PortalRoute: React.FC<PortalRouteProps>;
// Sem role → redireciona para /portal
// Role não permitida → redireciona para ROLE_DASHBOARDS[role]
// Role permitida → <Outlet />
```

<!-- supabase client existente -->
```typescript
// src/lib/supabase.ts
export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

<!-- App.tsx existente — rotas a NÃO alterar -->
// /admin → AdminLogin
// /admin/dashboard, /admin/editor, /admin/cases/:caseId → ProtectedRoute

<!-- ROLE_DASHBOARDS map (usar em ambos PortalLogin e PortalRoute) -->
const ROLE_DASHBOARDS: Record<string, string> = {
  admin: '/portal/admin',
  dev: '/portal/dev',
  presidente: '/portal/presidente',
  user: '/portal/user',
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar AuthContext e PortalRoute</name>
  <files>src/contexts/AuthContext.tsx, src/components/PortalRoute.tsx</files>
  <action>
Criar dois arquivos — AuthContext e PortalRoute — seguindo exatamente os contratos acima.

**src/contexts/AuthContext.tsx:**
- Importar Session do @supabase/supabase-js para tipagem correta
- `loading` começa `true` e só fica `false` quando AMBOS session E role estão resolvidos (evita race condition)
- `fetchRole` busca `role` e `subsection_id` em `user_roles` filtrando por `user_id = session.user.id` e `active = true`
- Se user_roles retornar null (usuário sem role configurada), setar role como null e loading false — não travar
- `signOut` chama `supabase.auth.signOut()` e limpa role/subsectionId
- `onAuthStateChange` atualiza session E re-fetch de role quando sessão muda
- Exportar `AuthProvider` (componente) e `useAuth` (hook)

**src/components/PortalRoute.tsx:**
- Importar `useAuth` do AuthContext
- Se `loading === true`: retornar spinner (usar mesmo padrão de ProtectedRoute existente: `<Loader2 className="h-8 w-8 animate-spin text-primary" />` centralizado)
- Se `!session`: `<Navigate to="/portal" replace />`
- Se role não está em `allowedRoles`: `<Navigate to={ROLE_DASHBOARDS[role ?? ''] ?? '/portal'} replace />`
- Caso contrário: `<Outlet />`
- ROLE_DASHBOARDS map definido no topo do arquivo como constante
  </action>
  <verify>
    <automated>
      node -e "const fs = require('fs'); ['src/contexts/AuthContext.tsx', 'src/components/PortalRoute.tsx'].forEach(f => { const c = fs.readFileSync(f, 'utf8'); console.log(f + ' exists (' + c.length + ' chars)'); }); const ac = fs.readFileSync('src/contexts/AuthContext.tsx', 'utf8'); ['AuthProvider', 'useAuth', 'loading', 'fetchRole', 'signOut', 'onAuthStateChange'].forEach(k => { if (!ac.includes(k)) throw new Error('AuthContext missing: ' + k); }); const pr = fs.readFileSync('src/components/PortalRoute.tsx', 'utf8'); ['ROLE_DASHBOARDS', 'allowedRoles', 'loading', 'Navigate'].forEach(k => { if (!pr.includes(k)) throw new Error('PortalRoute missing: ' + k); }); console.log('Both files valid');"
    </automated>
  </verify>
  <done>AuthContext.tsx com AuthProvider + useAuth() exportados; PortalRoute.tsx com guard de role e ROLE_DASHBOARDS; loading=true até session+role resolvidos</done>
</task>

<task type="auto">
  <name>Task 2: Criar PortalLogin e adicionar rotas do portal em App.tsx</name>
  <files>src/pages/portal/PortalLogin.tsx, src/App.tsx</files>
  <action>
**src/pages/portal/PortalLogin.tsx:**
Criar a pasta `src/pages/portal/` se não existir.

Página de login para o portal interno. Reusar exatamente o mesmo estilo visual de `AdminLogin.tsx` (Card centrado em min-h-screen bg-gray-100), mas:
- Título: "Portal OAB-MA" (não "Admin Login")
- Usar `react-hook-form` + `zod` para validação do formulário (schema: email required, password min 6 chars)
- Após login bem-sucedido, buscar role em `user_roles` e redirecionar para `ROLE_DASHBOARDS[role]`
- Se role for null (usuário sem configuração), exibir toast de erro: "Seu acesso ainda não foi configurado. Contate o administrador."
- Se usuário já está logado quando acessa /portal, redirecionar para o dashboard correto (verificar em useEffect com supabase.auth.getSession)
- Exibir mensagem de erro humanizada para credenciais inválidas: "Email ou senha incorretos"
- Não navegar para /admin/dashboard — portal e admin são fluxos separados

```typescript
// Zod schema:
const loginSchema = z.object({
  email: z.string().email('Email inválido'),
  password: z.string().min(6, 'Senha deve ter ao menos 6 caracteres'),
});
```

ROLE_DASHBOARDS definido localmente (mesma constante).

**src/App.tsx:**
Adicionar imports e rotas do portal SEM alterar nenhuma das rotas existentes. Seguir o padrão do research (AuthProvider dentro do BrowserRouter, scoped apenas às rotas do portal):

```tsx
// Adicionar no topo com outros imports:
import { AuthProvider } from './contexts/AuthContext';
import PortalLogin from './pages/portal/PortalLogin';
import { PortalRoute } from './components/PortalRoute';

// Adicionar ANTES das rotas públicas, depois das rotas /admin:
<Route path="/portal" element={<PortalLogin />} />
<Route element={<AuthProvider><Outlet /></AuthProvider>}>
  <Route element={<PortalRoute allowedRoles={['admin']} />}>
    <Route path="/portal/admin" element={<div>Admin Dashboard — Em breve</div>} />
  </Route>
  <Route element={<PortalRoute allowedRoles={['dev']} />}>
    <Route path="/portal/dev" element={<div>Dev Dashboard — Em breve</div>} />
  </Route>
  <Route element={<PortalRoute allowedRoles={['presidente']} />}>
    <Route path="/portal/presidente" element={<div>Presidente Dashboard — Em breve</div>} />
  </Route>
  <Route element={<PortalRoute allowedRoles={['user']} />}>
    <Route path="/portal/user" element={<div>User Dashboard — Em breve</div>} />
  </Route>
</Route>
```

Os placeholders `<div>... Em breve</div>` serão substituídos nos planos 03-03 e 03-04.

ATENÇÃO: AuthProvider NÃO deve ser colocado como wrapper global — apenas nas rotas /portal/*. As rotas /admin/* continuam usando o ProtectedRoute existente sem alteração.
  </action>
  <verify>
    <automated>
      node -e "const fs = require('fs'); const login = fs.readFileSync('src/pages/portal/PortalLogin.tsx', 'utf8'); ['react-hook-form', 'zod', 'ROLE_DASHBOARDS', 'Portal OAB-MA', 'Contate o administrador'].forEach(k => { if (!login.includes(k)) throw new Error('PortalLogin missing: ' + k); }); const app = fs.readFileSync('src/App.tsx', 'utf8'); ['/portal', 'AuthProvider', 'PortalRoute', 'PortalLogin'].forEach(k => { if (!app.includes(k)) throw new Error('App.tsx missing: ' + k); }); if (!app.includes('AdminLogin')) throw new Error('App.tsx: AdminLogin removed (should still be there)'); console.log('PortalLogin and App.tsx valid');"
    </automated>
  </verify>
  <done>PortalLogin.tsx com formulário validado por zod, redirecionamento por role, e mensagem de erro para usuário sem role. App.tsx com rotas /portal/* adicionadas, AuthProvider scoped ao portal, rotas /admin/* intocadas.</done>
</task>

</tasks>

<verification>
Verificação funcional — executar `npm run dev` e testar manualmente:
1. Acessar `/portal` — deve exibir formulário "Portal OAB-MA" (não a tela admin)
2. Acessar `/admin` — deve exibir a tela admin original sem alteração
3. Tentar acessar `/portal/admin` sem login — deve redirecionar para `/portal`
4. Login com credenciais inválidas — deve exibir toast de erro humanizado
</verification>

<success_criteria>
- `src/contexts/AuthContext.tsx` exporta AuthProvider e useAuth, tem loading=true até session+role resolvidos
- `src/components/PortalRoute.tsx` redireciona por role usando ROLE_DASHBOARDS
- `src/pages/portal/PortalLogin.tsx` usa react-hook-form+zod, redireciona por role após login
- `src/App.tsx` tem rotas /portal/* com AuthProvider scoped, rotas /admin/* inalteradas
- `npm run build` passa sem erros de TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-access-portal-internal-management/03-02-SUMMARY.md`
</output>
