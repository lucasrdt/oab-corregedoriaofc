---
phase: 01-identity-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/config/template-ivaldo.ts
  - src/contexts/SiteContext.tsx
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-05

must_haves:
  truths:
    - "O site exibe azul OAB-MA (#0d53de) como cor primária em botões, links, títulos e scrollbar — sem flash de verde escuro após carregamento"
    - "O site exibe vermelho OAB-MA (#e00b0b) como cor secundária em destaques e elementos accent — sem flash de dourado após carregamento"
    - "Componentes que usavam bg-secondary como fundo neutro (ex: TabsList, Badge secondary) não exibem fundo vermelho"
    - "Tipografia usa Roboto com hierarquia clara: headings via font-heading, body via font-body, tamanhos consistentes entre seções"
    - "Espaçamentos usam as classes utilitárias .section-padding / .container-padding / .section-title em vez de valores arbitrários"
    - "O site público renderiza corretamente em telas móveis (320px–768px) sem quebra de layout em menu, cards e formulários"
  artifacts:
    - path: "src/index.css"
      provides: "CSS variables OAB-MA hardcoded em :root"
      contains: "--primary: 220 76% 46%"
    - path: "src/config/template-ivaldo.ts"
      provides: "Template default com cores OAB-MA"
      contains: "primary: \"#0d53de\""
    - path: "src/contexts/SiteContext.tsx"
      provides: "SiteContext simplificado sem injeção dinâmica de cores e sem RPC de domínio"
      contains: "fullIvaldoTemplate"
  key_links:
    - from: "src/index.css :root"
      to: "hsl(var(--primary)) em todos os componentes"
      via: "CSS custom properties"
      pattern: "--primary: 220 76% 46%"
    - from: "src/contexts/SiteContext.tsx fetchConfig"
      to: "fullIvaldoTemplate"
      via: "setConfig(fullIvaldoTemplate) — sem RPC call"
      pattern: "setConfig\\(fullIvaldoTemplate\\)"
---

<objective>
Aplicar identidade visual OAB-MA via CSS variables estáticas e eliminar a injeção dinâmica de cores do SiteContext.

Purpose: O site atualmente exibe verde escuro (#113d3e) e dourado (#e8aa42) injetados dinamicamente via Supabase RPC. Após esta tarefa, as cores corretas da OAB-MA são hardcoded no CSS e o SiteContext não sobrescreve mais os valores de :root.

Output:
- src/index.css com --primary e --secondary OAB-MA em :root
- src/config/template-ivaldo.ts com cores OAB-MA no objeto colors
- src/contexts/SiteContext.tsx sem hexToHsl injection e sem get_site_by_domain RPC
- Auditoria de bg-secondary incorreto substituída por bg-muted onde necessário
</objective>

<execution_context>
@C:/Users/gusta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-foundation/01-RESEARCH.md
</context>

<interfaces>
<!-- Contratos chave que o executor precisa. Extraídos do codebase. -->

De src/index.css (:root atual — valores a substituir):
```css
--primary: 184 57% 15%;   /* SUBSTITUIR por: 220 76% 46% */
--secondary: 41 73% 57%;  /* SUBSTITUIR por: 0 77% 46% */
--ring: 184 57% 15%;      /* SUBSTITUIR para acompanhar primary: 220 76% 46% */
/* dark mode também tem --primary e --secondary — atualizar coerentemente */
```

De src/config/template-ivaldo.ts (objeto colors — valores a substituir):
```typescript
colors: {
  primary: "#113d3e",   // SUBSTITUIR por: "#0d53de"
  secondary: "#e8aa42", // SUBSTITUIR por: "#e00b0b"
  ...
}
```

De src/contexts/SiteContext.tsx (blocos a remover/simplificar):
```typescript
// REMOVER: fetchConfig inteiro (RPC get_site_by_domain + domain resolution)
// SUBSTITUIR por: setConfig(fullIvaldoTemplate); setLoading(false);

// REMOVER: useEffect de color injection (hexToHsl + root.style.setProperty)
// Manter: useEffect de SEO metadata (document.title, favicon, meta description)
// Manter: SiteProvider, useSite export, SiteContextType interface
// Manter: siteId em estado, mas sempre null (sem RPC, não há UUID disponível)
```

De src/components/ui/sidebar.tsx (exports disponíveis):
```typescript
export { Sidebar, SidebarContent, SidebarProvider, SidebarMenu,
         SidebarMenuItem, SidebarMenuButton, SidebarTrigger,
         SidebarHeader, SidebarFooter, SidebarGroup,
         SidebarGroupLabel, SidebarGroupContent, SidebarInset }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Hardcode cores OAB-MA em :root e atualizar template default</name>
  <files>src/index.css, src/config/template-ivaldo.ts</files>
  <action>
    **src/index.css** — No bloco `:root` dentro de `@layer base`, substituir os valores das variáveis de cor:

    ANTES:
    ```css
    --primary: 184 57% 15%;
    --primary-foreground: 0 0% 100%;
    --secondary: 41 73% 57%;
    --secondary-foreground: 0 0% 0%;
    --accent: 40 45% 80%;
    --accent-foreground: 184 57% 15%;
    --ring: 184 57% 15%;
    ```

    DEPOIS:
    ```css
    --primary: 220 76% 46%;          /* OAB-MA blue #0d53de */
    --primary-foreground: 0 0% 100%; /* white */
    --secondary: 0 77% 46%;          /* OAB-MA red #e00b0b */
    --secondary-foreground: 0 0% 100%; /* white (red bg needs white text) */
    --accent: 220 76% 92%;           /* azul bem claro para accent neutro */
    --accent-foreground: 220 76% 25%;
    --ring: 220 76% 46%;             /* acompanha primary */
    ```

    Remover o comentário `/* Default: Verde escuro */` e `/* Default: Dourado */`.
    Atualizar o bloco `.dark` analogamente: `--primary: 220 76% 65%` (versão mais clara para dark mode), `--secondary: 0 77% 60%`.

    **Atenção ao bg-secondary:** O token `secondary` agora é vermelho. Auditar no mesmo arquivo todas as classes que usam `secondary` como fundo neutro e corrigir:
    - Se alguma classe utilitária no arquivo usa `bg-secondary` para fundo neutro, trocar por `bg-muted`.

    **src/config/template-ivaldo.ts** — Dentro do objeto `colors`, substituir:
    ```typescript
    primary: "#0d53de",   // OAB-MA azul
    secondary: "#e00b0b", // OAB-MA vermelho
    background: "#fcfdfd", // mantém
    textDark: "#1a1a1a",   // mantém
    textLight: "#6b7280",  // mantém
    ```

    Atualizar o `name` e `seo.title` para refletir OAB-MA:
    ```typescript
    name: "Corregedoria Geral OAB-MA",
    seo: {
      title: "Corregedoria Geral - OAB Maranhão",
      description: "Corregedoria Geral da Ordem dos Advogados do Brasil - Seccional Maranhão.",
      ...
    }
    ```
    Manter todos os outros campos do template intactos — conteúdo é escopo da Phase 2.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build sem erros TypeScript. As variáveis --primary e --secondary em src/index.css contêm os valores HSL OAB-MA. O objeto colors em template-ivaldo.ts contém "#0d53de" e "#e00b0b".</done>
</task>

<task type="auto">
  <name>Task 2: Simplificar SiteContext — remover RPC e injeção dinâmica de cores</name>
  <files>src/contexts/SiteContext.tsx</files>
  <action>
    Substituir a função `fetchConfig` inteira (que faz `supabase.rpc("get_site_by_domain", ...)`) por uma versão estática:

    ```typescript
    const fetchConfig = useCallback(async () => {
      setLoading(true);
      setError(null);
      setSiteId(null);
      // Single-tenant: usar template estático OAB-MA
      setConfig(fullIvaldoTemplate);
      setLoading(false);
    }, []);
    ```

    Remover o `useEffect` de injeção de cores (o bloco que contém `hexToHsl`, `root.style.setProperty('--primary', hsl)` e `root.style.setProperty('--secondary', hsl)`). As cores agora vêm de `:root` no CSS. Manter o `useEffect` de SEO metadata intocado (document.title, favicon, meta description) — ele continua funcional.

    Remover as importações que não são mais usadas após essa simplificação. Manter:
    - `import { supabase } from '@/lib/supabase'` pode ser removido se não houver mais nenhuma referência.
    - `fullIvaldoTemplate` continua necessário.
    - `skeletonTemplate` não é mais necessário para o estado inicial — mudar `useState<TemplateIvaldo>(skeletonTemplate)` para `useState<TemplateIvaldo>(fullIvaldoTemplate)` para evitar flash de conteúdo vazio.

    Manter intactos: `SiteContextType`, `SiteContext`, `SiteProvider`, `useSite`, `deepMerge`, `isObject`, o `useEffect` de SEO, o render condicional de loading/error.

    **Atenção ao Pitfall 2 (research):** O `useEffect` de cores usava `inline style` com `setProperty`, que tem maior especificidade que a stylesheet. Removendo esse `useEffect`, os valores de `:root` da stylesheet passam a ser definitivos. Confirmar que o bloco foi removido completamente (não apenas comentado).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build sem erros. SiteContext não faz mais chamada RPC. O arquivo não contém mais `hexToHsl` nem `setProperty('--primary'`. O estado inicial usa `fullIvaldoTemplate`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificar cores OAB-MA no browser</name>
  <action>Verificação humana — nenhuma ação de código necessária.</action>
  <what-built>
    CSS variables OAB-MA hardcoded em :root (azul #0d53de + vermelho #e00b0b), SiteContext simplificado sem RPC e sem injeção dinâmica de cores. Site público usa as novas cores desde o primeiro render.
  </what-built>
  <how-to-verify>
    1. Rodar `npm run dev` e abrir http://localhost:5173
    2. Verificar que botões primários, links e a scrollbar exibem azul OAB-MA (não verde escuro)
    3. Verificar que elementos de destaque/accent exibem vermelho OAB-MA (não dourado)
    4. Abrir DevTools > Elements > `<html>` — confirmar que `style` inline NÃO contém `--primary` ou `--secondary` (ausência confirma que a injeção foi removida)
    5. Recarregar a página 3 vezes — confirmar que as cores não trocam após o carregamento (sem flash de cores erradas)
    6. Em mobile (DevTools responsive, 375px): verificar que menu, cards e formulários da home não quebram layout
    7. Navegar para /equipe, /duvidas, /contato — verificar cores e responsividade básica
  </how-to-verify>
  <resume-signal>Digite "aprovado" se as cores estiverem corretas e sem flash. Descreva qualquer problema visual encontrado.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passa sem erros TypeScript
- src/index.css :root contém `--primary: 220 76% 46%` e `--secondary: 0 77% 46%`
- src/config/template-ivaldo.ts colors contém `"#0d53de"` e `"#e00b0b"`
- src/contexts/SiteContext.tsx não contém `hexToHsl` nem `get_site_by_domain` nem `setProperty`
- DevTools confirma ausência de inline style `--primary` no elemento `<html>`
</verification>

<success_criteria>
- Cores primária e secundária OAB-MA exibidas desde o primeiro render, sem flash de verde/dourado
- Nenhum componente exibe fundo vermelho inesperado (bg-secondary como neutro foi corrigido)
- Site público responsivo em 375px sem quebra de layout
- Build TypeScript passa sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-identity-foundation/01-01-SUMMARY.md`
</output>
