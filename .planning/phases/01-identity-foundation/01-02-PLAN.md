---
phase: 01-identity-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/pages/admin/AdminLayout.tsx
  - src/pages/admin/SiteEditor.tsx
  - src/pages/admin/Dashboard.tsx
  - src/App.tsx
  - src/pages/admin/components/CreateSiteModal.tsx
  - src/pages/admin/components/DeleteSiteModal.tsx
  - src/pages/admin/components/DnsInstructionsModal.tsx
  - src/lib/domainService.ts
autonomous: false

requirements:
  - UI-04
  - UI-06

must_haves:
  truths:
    - "O painel admin em /admin/dashboard exibe um menu lateral (sidebar) com os itens de navegação das seções do editor"
    - "Clicar em um item da sidebar rola ou navega para a seção correspondente do editor sem recarregar a página"
    - "Não existe nenhuma rota, botão ou link relacionado a criação ou troca de múltiplos sites no frontend"
    - "O login em /admin redireciona corretamente para /admin/dashboard após autenticação"
    - "A rota /admin/editor não contém mais parâmetro :id — é uma rota fixa"
    - "O SiteEditor salva configurações no Supabase usando um siteId fixo (sem depender de useParams)"
  artifacts:
    - path: "src/pages/admin/AdminLayout.tsx"
      provides: "Shell de layout admin com SidebarProvider + Sidebar"
      exports: ["AdminLayout"]
    - path: "src/pages/admin/SiteEditor.tsx"
      provides: "Editor sem useParams(':id'), seções acessíveis via sidebar"
      contains: "OAB_MA_SITE_ID"
    - path: "src/App.tsx"
      provides: "Rotas admin limpas sem siteId param"
      contains: "/admin/editor"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/admin/AdminLayout.tsx"
      via: "AdminLayout wrapping SiteEditor na rota /admin/editor"
      pattern: "AdminLayout"
    - from: "src/pages/admin/SiteEditor.tsx"
      to: "supabase.from('sites').update"
      via: "OAB_MA_SITE_ID constant em vez de useParams id"
      pattern: "OAB_MA_SITE_ID"
---

<objective>
Converter a navegação do painel admin de tabs horizontais para sidebar lateral e remover toda a lógica multi-tenant do frontend.

Purpose: O admin atual tem 9 tabs em grid que quebram em telas menores e expõe operações de multi-tenant (criar/deletar sites, gerenciar DNS Vercel). Esta tarefa cria o AdminLayout com sidebar e apaga todos os arquivos e rotas multi-tenant.

Output:
- src/pages/admin/AdminLayout.tsx — novo shell de layout com shadcn/ui Sidebar
- src/pages/admin/SiteEditor.tsx — convertido de tabs para seções navegadas pela sidebar
- src/pages/admin/Dashboard.tsx — substituído por componente AdminHome simples (sem multi-tenant)
- src/App.tsx — rotas admin limpas
- Deletados: CreateSiteModal.tsx, DeleteSiteModal.tsx, DnsInstructionsModal.tsx, domainService.ts
</objective>

<execution_context>
@C:/Users/gusta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gusta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-foundation/01-RESEARCH.md
@.planning/phases/01-identity-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos chave que o executor precisa. Extraídos do codebase. -->

De src/components/ui/sidebar.tsx (exports disponíveis):
```typescript
export {
  Sidebar, SidebarContent, SidebarProvider, SidebarMenu,
  SidebarMenuItem, SidebarMenuButton, SidebarTrigger,
  SidebarHeader, SidebarFooter, SidebarGroup,
  SidebarGroupLabel, SidebarGroupContent, SidebarInset,
  useSidebar
}
```

De src/pages/admin/SiteEditor.tsx (tabs atuais — tornam-se itens da sidebar):
```
value="general"   → label: "Geral & SEO"
value="content"   → label: "Quem Somos"
value="team"      → label: "Equipe"
value="cases"     → label: "Casos"
value="faq"       → label: "FAQ"
value="leads"     → label: "Leads"
value="calendar"  → label: "Agenda"
value="articles"  → label: "Notícias"
value="advanced"  → label: "Avançado"
```

De src/App.tsx (rotas admin atuais — o que muda):
```typescript
// REMOVER:
<Route path="/admin/editor/:siteId/cases/:caseId" .../>  // siteId-scoped
<Route path="/admin/editor/:id" element={<SiteEditor />} />

// MANTER com novo componente:
<Route path="/admin/dashboard" element={<AdminHome />} />  // era Dashboard multi-tenant

// ADICIONAR:
<Route path="/admin/editor" element={<AdminLayout><SiteEditor /></AdminLayout>} />
<Route path="/admin/cases/:caseId" element={<CaseDetailsEditor />} />
```

De src/pages/admin/Dashboard.tsx (a ser substituído):
```typescript
// Este arquivo lê supabase.from('sites').select('*')
// e renderiza lista de sites com botões criar/deletar/verificar DNS
// SUBSTITUIR por AdminHome simples: link para /admin/editor + estatísticas básicas
```

Constante necessária em SiteEditor (substitui useParams):
```typescript
// O siteId da OAB-MA no Supabase — executar durante a task:
// SELECT id FROM sites WHERE domain = 'ivaldopraddo.com' (ou verificar no Dashboard atual)
// Se não disponível, usar env var: import.meta.env.VITE_SITE_ID
const OAB_MA_SITE_ID = import.meta.env.VITE_SITE_ID || 'placeholder-uuid';
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar AdminLayout com sidebar e converter SiteEditor de tabs para seções</name>
  <files>src/pages/admin/AdminLayout.tsx, src/pages/admin/SiteEditor.tsx</files>
  <action>
    **PRIMEIRO: Descobrir o siteId OAB-MA.**
    No Supabase (ou lendo o arquivo `.env` / `src/lib/supabase.ts`), descobrir o UUID do site OAB-MA.
    Se o projeto tiver `.env` ou `.env.local`, verificar se existe `VITE_SITE_ID`.
    Se não existir, criar `.env.local` com `VITE_SITE_ID=<uuid-encontrado>` ou usar o site registrado no Supabase.
    Se não for possível determinar o UUID agora, usar `'CONFIGURE_SITE_ID'` como placeholder visível e documentar na SUMMARY que é necessário configurar.

    **Criar src/pages/admin/AdminLayout.tsx:**
    ```tsx
    import { SidebarProvider, Sidebar, SidebarContent, SidebarHeader,
             SidebarMenu, SidebarMenuItem, SidebarMenuButton,
             SidebarTrigger, SidebarInset } from '@/components/ui/sidebar';
    import { useNavigate } from 'react-router-dom';
    import { LayoutDashboard } from 'lucide-react';

    const navItems = [
      { id: 'general',  label: 'Geral & SEO' },
      { id: 'content',  label: 'Quem Somos' },
      { id: 'team',     label: 'Equipe' },
      { id: 'cases',    label: 'Casos' },
      { id: 'faq',      label: 'FAQ' },
      { id: 'leads',    label: 'Leads' },
      { id: 'calendar', label: 'Agenda' },
      { id: 'articles', label: 'Notícias' },
      { id: 'advanced', label: 'Avançado' },
    ];

    const AdminLayout = ({ children, activeSection, onSectionChange }: {
      children: React.ReactNode;
      activeSection?: string;
      onSectionChange?: (id: string) => void;
    }) => {
      const navigate = useNavigate();
      return (
        <SidebarProvider>
          <div className="flex min-h-screen w-full">
            <Sidebar>
              <SidebarHeader className="p-4 border-b">
                <button onClick={() => navigate('/admin/dashboard')}
                  className="flex items-center gap-2 text-sm font-semibold text-primary hover:opacity-80">
                  <LayoutDashboard className="w-4 h-4" />
                  Corregedoria OAB-MA
                </button>
              </SidebarHeader>
              <SidebarContent>
                <SidebarMenu className="px-2 py-2">
                  {navItems.map(item => (
                    <SidebarMenuItem key={item.id}>
                      <SidebarMenuButton
                        isActive={activeSection === item.id}
                        onClick={() => onSectionChange?.(item.id)}
                        className="w-full text-left"
                      >
                        {item.label}
                      </SidebarMenuButton>
                    </SidebarMenuItem>
                  ))}
                </SidebarMenu>
              </SidebarContent>
            </Sidebar>
            <SidebarInset>
              <header className="flex h-14 items-center gap-2 border-b px-4">
                <SidebarTrigger />
              </header>
              <main className="flex-1 p-6">{children}</main>
            </SidebarInset>
          </div>
        </SidebarProvider>
      );
    };

    export default AdminLayout;
    ```

    **Modificar src/pages/admin/SiteEditor.tsx:**
    1. Remover `import { useParams }` e a linha `const { id } = useParams();`
    2. Adicionar no topo do componente: `const OAB_MA_SITE_ID = import.meta.env.VITE_SITE_ID || 'CONFIGURE_SITE_ID';`
    3. Substituir todas as referências a `id` (de useParams) por `OAB_MA_SITE_ID`
    4. Adicionar estado de seção ativa: `const [activeSection, setActiveSection] = useState('general');`
    5. Substituir `<Tabs ...>` / `<TabsList ...>` / `<TabsTrigger ...>` pelo componente `AdminLayout`:
       ```tsx
       // Envolver o conteúdo existente em AdminLayout:
       return (
         <AdminLayout activeSection={activeSection} onSectionChange={setActiveSection}>
           {/* Barra de título com botão salvar */}
           <div className="flex items-center justify-between mb-6">
             <h1 className="text-xl font-semibold">Editor do Site</h1>
             <Button onClick={handleSave} disabled={saving}>
               <Save className="w-4 h-4 mr-2" />
               {saving ? 'Salvando...' : 'Salvar Alterações'}
             </Button>
           </div>
           {/* Renderizar apenas a seção ativa */}
           {activeSection === 'general' && <div>...conteúdo geral...</div>}
           {activeSection === 'content' && <div>...conteúdo quem somos...</div>}
           {/* etc para cada seção */}
         </AdminLayout>
       );
       ```
    6. Remover imports de `Tabs, TabsContent, TabsList, TabsTrigger` — não usados mais.
    7. Adicionar import: `import AdminLayout from './AdminLayout';`
    8. Remover o botão "Voltar" (ArrowLeft) que navegava para /admin/dashboard — o link está agora no header da sidebar.

    **Atenção ao Pitfall 5 (research):** SiteEditor passa `siteId` para sub-editors (TeamEditor, CasesEditor, etc.) como prop. Substituir todos os `siteId={id}` por `siteId={OAB_MA_SITE_ID}`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Build passa. AdminLayout.tsx existe e exporta default. SiteEditor.tsx não importa useParams nem Tabs. SiteEditor usa OAB_MA_SITE_ID em vez de useParams id.</done>
</task>

<task type="auto">
  <name>Task 2: Remover multi-tenant — deletar arquivos, substituir Dashboard, limpar App.tsx</name>
  <files>src/pages/admin/Dashboard.tsx, src/App.tsx, src/pages/admin/components/CreateSiteModal.tsx, src/pages/admin/components/DeleteSiteModal.tsx, src/pages/admin/components/DnsInstructionsModal.tsx, src/lib/domainService.ts</files>
  <action>
    **Deletar os seguintes arquivos** (remover completamente do disco):
    - `src/pages/admin/components/CreateSiteModal.tsx`
    - `src/pages/admin/components/DeleteSiteModal.tsx`
    - `src/pages/admin/components/DnsInstructionsModal.tsx`
    - `src/lib/domainService.ts`

    **Substituir src/pages/admin/Dashboard.tsx** por um componente AdminHome simples:
    ```tsx
    import { useNavigate } from 'react-router-dom';
    import { Button } from '@/components/ui/button';
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
    import { Edit, LogOut } from 'lucide-react';
    import { supabase } from '@/lib/supabase';

    const Dashboard = () => {
      const navigate = useNavigate();

      const handleLogout = async () => {
        await supabase.auth.signOut();
        navigate('/admin');
      };

      return (
        <div className="min-h-screen bg-background p-8">
          <div className="max-w-2xl mx-auto">
            <div className="flex items-center justify-between mb-8">
              <h1 className="text-2xl font-bold text-primary">
                Corregedoria Geral OAB-MA
              </h1>
              <Button variant="outline" size="sm" onClick={handleLogout}>
                <LogOut className="w-4 h-4 mr-2" />
                Sair
              </Button>
            </div>
            <Card>
              <CardHeader>
                <CardTitle>Painel Administrativo</CardTitle>
              </CardHeader>
              <CardContent className="space-y-4">
                <p className="text-muted-foreground">
                  Gerencie o conteúdo do site da Corregedoria Geral.
                </p>
                <Button onClick={() => navigate('/admin/editor')} className="w-full sm:w-auto">
                  <Edit className="w-4 h-4 mr-2" />
                  Editar Conteúdo do Site
                </Button>
              </CardContent>
            </Card>
          </div>
        </div>
      );
    };

    export default Dashboard;
    ```

    **Modificar src/App.tsx:**
    1. Remover imports de `Dashboard` do arquivo antigo (não é necessário reimportar — o arquivo foi substituído no mesmo caminho)
    2. Remover o import de `CreateSiteModal`, `DeleteSiteModal`, `DnsInstructionsModal` se existirem em App.tsx
    3. Substituir as rotas admin pelo novo conjunto:
       ```tsx
       import AdminLayout from './pages/admin/AdminLayout';

       // Dentro de <Routes>:
       <Route path="/admin" element={<AdminLogin />} />
       <Route element={<ProtectedRoute />}>
         <Route path="/admin/dashboard" element={<Dashboard />} />
         <Route path="/admin/editor" element={<SiteEditor />} />
         <Route path="/admin/cases/:caseId" element={<CaseDetailsEditor />} />
       </Route>
       ```
    4. Remover a rota `/admin/editor/:id` (com param)
    5. Remover a rota `/admin/editor/:siteId/cases/:caseId` (multi-tenant scoped)
    6. Remover qualquer import de `domainService`

    **Atenção ao Pitfall 3 (research):** O path `/admin/dashboard` é mantido — apenas o componente que ele renderiza mudou. AdminLogin.tsx redireciona para `/admin/dashboard` após login e isso continua funcionando.

    **Atenção ao Pitfall 4 (research):** A rota `/:slug` existe como catch-all. Todas as rotas admin devem ter o prefixo `/admin`. Verificar no App.tsx final que todas as novas rotas começam com `/admin`.

    Após as modificações, verificar que `src/App.tsx` não tem imports não utilizados (Vite vai alertar).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Build passa sem erros. Os 4 arquivos multi-tenant não existem mais no disco. App.tsx não contém `:siteId` nem `:id` nas rotas admin. Dashboard.tsx não importa CreateSiteModal, DeleteSiteModal, DnsInstructionsModal nem domainService.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificar sidebar e ausência de multi-tenant</name>
  <action>Verificação humana — nenhuma ação de código necessária.</action>
  <what-built>
    Painel admin com sidebar lateral (9 itens de navegação), multi-tenant completamente removido (sem criar/deletar sites, sem DNS Vercel). Login redireciona para /admin/dashboard. Editor acessível via /admin/editor com navegação por sidebar.
  </what-built>
  <how-to-verify>
    1. Rodar `npm run dev`
    2. Acessar http://localhost:5173/admin — fazer login
    3. Confirmar que /admin/dashboard exibe o novo painel simples (sem lista de sites, sem botão "Criar Site")
    4. Clicar em "Editar Conteúdo do Site" — confirmar que /admin/editor carrega com sidebar lateral visível
    5. Clicar em cada item da sidebar (Geral & SEO, Equipe, FAQ, etc.) — confirmar que a seção correspondente é exibida
    6. Em mobile (DevTools 375px): confirmar que a sidebar colapsa e o botão hambúrguer (SidebarTrigger) aparece no header
    7. Confirmar que nenhuma URL do tipo /admin/editor/algum-id existe ou é acessível (deve redirecionar para 404 ou /admin)
    8. Verificar no DevTools > Network que nenhuma chamada RPC `get_site_by_domain` é feita ao carregar o admin
  </how-to-verify>
  <resume-signal>Digite "aprovado" se a sidebar funciona e o multi-tenant foi removido. Descreva qualquer problema encontrado.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passa sem erros TypeScript
- Arquivos deletados não existem: CreateSiteModal.tsx, DeleteSiteModal.tsx, DnsInstructionsModal.tsx, domainService.ts
- App.tsx não contém `/admin/editor/:id` nem `/admin/editor/:siteId`
- SiteEditor.tsx não contém `useParams` nem `Tabs`
- AdminLayout.tsx exporta default e usa SidebarProvider
- Dashboard.tsx não importa domainService nem modais multi-tenant
</verification>

<success_criteria>
- Painel admin exibe sidebar lateral com 9 itens de navegação
- Nenhum rastro de multi-tenant (criar/deletar sites, DNS Vercel) no frontend
- Login redireciona corretamente para /admin/dashboard
- SiteEditor salva usando siteId fixo (sem param de URL)
- Build TypeScript limpo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-identity-foundation/01-02-SUMMARY.md`
</output>
